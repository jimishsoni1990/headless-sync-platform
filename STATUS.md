# HSP — Progress Status

> **Standing instruction:** Update this file at the end of every working session: flip task states,
> set last-updated, set next action. This is the session-to-session source of progress truth.
>
> Rationale and architecture live in [`docs/IMPLEMENTATION_PLAN.md`](docs/IMPLEMENTATION_PLAN.md)
> and [`docs/ARCHITECTURE_DECISIONS.md`](docs/ARCHITECTURE_DECISIONS.md) — do not duplicate them here.

---

**Current phase:** Phase 1A — Blog MVP  
**Last updated:** 2026-07-13 (GATE-S3 attempt — **STOP-and-flag, criterion 3 (Reconciliation) unsatisfiable → FLAG-GATES3-1 raised**. Pre-implementation review found: GATE-S3 criteria 1 (worker health / heartbeat-cycle failure detection — DECISION P) and 2 (DLQ payload snapshot + stack trace — OPEN-3) are backed by real production code and are validatable. Criterion 3 (Reconciliation — ADR-026/ADR-027/ADR-045) **cannot pass**: `ReconciliationWorkerStrategy` is a STUB that returns `false` ([ReconciliationWorkerStrategy.php:23](headless-sync/core/Workers/Strategies/ReconciliationWorkerStrategy.php#L23)), registered as a stub in the DI container; a full-codebase search finds no drift detection, no incremental validation, no full reconciliation, no WordPress-wins repair, no `hsp reconcile` command, and no schedule wiring. Reconciliation was never built — OPS-S1 shipped heartbeat/DLQ/replay/metrics only; Phase 3 (where it is deferred) is post-MVP; DECISION T ([ARCHITECTURE_DECISIONS.md:792](docs/ARCHITECTURE_DECISIONS.md#L792)) itself calls reconciliation "future" work. Per the GATE-S3 brief (NO production code changes; a failed check is a STOP-and-flag, not a fix; no `markTestIncomplete`), building reconciliation is out of scope and writing evidence against absent behavior is disallowed. This is a Session-Map sequencing conflict (GATE-S3 requires reconciliation to *execute* before any session builds it) needing an architect ruling. NO gate test written, NO production code changed. See FLAG-GATES3-1 for the three ruling options.) Real gate evidence (live MySQL + live PG, no `markTestIncomplete`) for all three §4 criteria in `tests/Integration/Gate/ScalabilityValidationTest.php`: (1) SKIP LOCKED verified with genuinely concurrent PG sessions — two physical links each hold a `FOR UPDATE` lock at the same instant, session B skips A's locked row to a different job under a 2 s statement_timeout (blocking would fail, not hang), plus a real-provider N=12 / 3-worker no-double-claim proof; (2) a stuck head job held by worker A does not block worker B from claiming + completing all 6 followers; (3) replay (DECISION T `replayEntity`) interleaved with a mid-flight normal-load batch in one queue/worker loop — both groups converge to correct state, `aggregate_versions` monotonic, zero duplicate `processed_events`. GATE-S2 suite 4/4 green (65 assertions); combined gate suite 10/10 green (121 assertions), deterministic on back-to-back re-run; full suite 892 tests green, 0 failures. No new flags. Tests-only change — no production code touched.)  
**Last updated:** 2026-07-13 (OPS-S3 Reconciliation MVP **SHIPPED** — DECISION U v1.19; FLAG-GATES3-1 resolved. Un-stubbed `ReconciliationWorkerStrategy` as a façade over a new core `ReconciliationService` (detector + orchestrator): three modes (hourly drift = timestamp/existence WP→PG; nightly incremental = window + checksum recompute; weekly full = whole corpus + checksum + PG→WP orphan sweep). Repair is DECISION T re-emission ONLY (`ReplayService::replayEntity`) — **no direct PG projection writes; WordPress wins by construction**. Detects missed captures (DECISION 1 backstop) and orphans (→ `.deleted` DECISION I tombstone). False-positive suppression (DECISION U D4): pending unrelayed `wp_hsp_outbox` row OR `system.events` version > `latest_processed_version` → in-flight, skip. New core: `WpReconciliationSourceInterface` + `SourceState` (contracts), `ReconciliationService` + `ReconciliationResult` + `ReconciliationCronRegistrar` (`core/Reconciliation/`), `ReconcileCommand` + `WpCliReconcileRegistrar` (`core/Cli/`). Module: `WpReconciliationSource` (`modules/Content/Reconciliation/`, WP reads + checksum recompute + pending-outbox — behind the core contract, Rule 5). WP-CLI `hsp reconcile drift|incremental|full|status`; WP-Cron triggers (cadence + page-size config-driven, `config/worker.php`); DI wired (ContentServiceProvider binds the source; WorkerServiceProvider binds service/strategy/command/cron; headless-sync.php registers CLI + cron). STEP 2b pre-check PASS: `content.*.updated_at` is source-derived (`post_modified_gmt`), not processing time — comparator sound. Tests: 25 new unit (comparator drift classes, suppression, mode windowing, taxonomy existence-only, dry-run, paging; strategy façade + execute() no-op; ReconcileCommand counter/routing) + 6 new live-MySQL+PG integration (missed-capture repair, both in-flight suppression paths, orphan tombstone, checksum-drift category rename, WordPress-wins with write-spy proving zero direct PG writes). Full suite 923 tests, 2183 assertions, 0 failures, 1 pre-existing P0-S5 deprecation; deterministic on two cold runs. Container smoke: all reconciliation bindings resolve via constructor injection (ADR-012). Verified: core imports no module (Rule 5), no new `pg_*` wrapper / no fifth handle (DECISION E/L Ruling 0), no schema change, `ReconciliationService` issues no DML.)  
**Last updated:** 2026-07-14 (GATE-S3 Operability Validation **PASS** — all three §4 criteria proven with real gate evidence on live MySQL + live PG in `tests/Integration/Gate/OperabilityValidationTest.php` (6 tests, 52 assertions). Criterion 1 (worker health / heartbeat-cycle crash detection — DECISION P): the real `WorkerEngine.tick()` + `DatabaseHeartbeatPublisher` write one current-state row per worker to `system.worker_heartbeats` (health visible, no history), and a `last_heartbeat_at`-age monitor query detects a crashed worker within one heartbeat cycle while leaving a freshly-ticked worker unflagged; derived `worker_count` (DECISION Q) confirms visibility. Criterion 2 (DLQ diagnostics — OPEN-3): a job driven to retry-limit exhaustion through the real `EventWorkerStrategy` dead-letters carrying a non-empty real PHP stack trace + a NOT-NULL structured JSONB payload snapshot (DECISION A), read back via `DeadLetterRepository::inspect` (`hsp dlq inspect`). Criterion 3 (reconciliation — ADR-026/027/045, DECISION U): all three modes (hourly drift, nightly incremental checksum, weekly full orphan sweep) detect + repair; every repair is DECISION T re-emission ONLY — a write-spy proves zero direct PG projection writes and zero write-transactions on the repair path, so WordPress wins by construction; plus idempotency + in-flight (D4) suppression. Combined gate suite 16/16 green (173 assertions), deterministic on two cold runs; full suite 929 tests green, 0 failures (was 923 + 6 GATE-S3), sole deprecation is the pre-existing P0-S5 Unit-suite one. Tests-only change — no production code touched. FLAG-GATES3-1 RESOLVED. No new flags.)  
**Last updated:** 2026-07-14 (maintenance — deprecation cleanup, tests-only. Fixed the sole PHPUnit deprecation carried since P0-S5: `DatabaseQueueProviderTest::test_valid_partitions_are_accepted` bound its data provider via a `/** @dataProvider validPartitionProvider */` doc-comment, which PHPUnit 11.5 deprecates ("Metadata in doc-comments is deprecated and will no longer be supported in PHPUnit 12"). Replaced with the `#[DataProvider('validPartitionProvider')]` attribute + `use PHPUnit\Framework\Attributes\DataProvider;` import — behavior-preserving (same three rows: content/commerce/system). Full Unit suite now 807/807 green with **0 deprecations** (previously ran with 1). Test-only change — no production code, contract, or migration touched. This was the last `@dataProvider` doc-comment in the tree; the suite is now clean for the PHPUnit 12 upgrade.)  
**Last updated:** 2026-07-14 (GATE-S4 Extensibility Validation **PASS** — the Architecture Validation Gate is now COMPLETE (all four sub-gates green). All three §4 Extensibility criteria proven with real gate evidence on live PostgreSQL in `tests/Integration/Gate/ExtensibilityValidationTest.php` (3 tests, 28 assertions). Each test proves the POSITIVE half against the real runtime AND asserts the NEGATIVE half mechanically via `git status --porcelain -- <path>` (catches both tracked modifications and new untracked files; an empty result is the evidence the guarded area was untouched — verified to have teeth: temporarily editing a `core/` file makes criterion 1 fail). Criterion 1 (add a new content field without modifying `core/`): a module-scoped transformer adds a `reading_time` field via the EXISTING `CanonicalPost.meta` extension point; the REAL `PostAdapter` carries it into `content.posts.meta_jsonb` with zero core AND zero adapter edits — asserts zero changes under `core/`. Criterion 2 (add a new projection column without modifying transformer or canonical model): the REAL, UNMODIFIED `PostTransformer` + `CanonicalPost` produce the canonical model; a test-scoped adapter derives a NEW real column `word_count` from it and writes a throwaway `content.posts_ext` projection — asserts zero changes to `PostTransformer.php` and `CanonicalPost.php`. Criterion 3 (add a new API resource without modifying existing endpoints): a new `AuthorQueryProvider` + `AuthorResource` implementing the EXISTING core `QueryProviderInterface`/`ResourceInterface` serve a live `content.authors` table (list + findBySlug + envelope shape) — asserts zero changes to `ContentRestRegistrar.php`, `Resources/`, and `Queries/`. All additions are test-scoped throwaways (test schemas/doubles): the extension SEAMS are what is proven, not Phase 1B features. Notable finding (not a defect): the real `PostAdapter` type-checks for `CanonicalPost` specifically, so criterion 1's true seam is the `meta` map on the existing canonical model, not a new canonical subclass — the test reflects this. Combined gate suite 19/19 green (201 assertions), deterministic on two consecutive cold runs; full suite 932 tests green, 0 failures (was 929 + 3 GATE-S4), 0 deprecations. Tests-only change — no production code touched. No new flags.)  
**Last updated:** 2026-07-15 (OPSC-S0 Doc 12 Ratification **SHIPPED** — docs-only; DECISION V; FLAG-PLANOPS1-1..11 RESOLVED. Adopted Doc 12 (Admin Operations Console) into the frozen record, conservatively scoped. **ARCHITECTURE_DECISIONS.md v1.19→v1.20:** added DECISION V (Operations Console Adoption) recording all eleven rulings verbatim in substance — (a) FLAG-3 B server-rendered PHP + minimal vanilla JS + WP native admin, **no node/npm/bundler**, React deferred to a future ADR that must not alter the provider/registry architecture; (b) FLAG-4 A PSR-12 platform-wide + WPCS security (escape/sanitize/capability/nonce) at WordPress entry points only; (c) FLAG-5 A all console metrics derived on-demand per DECISION Q, zero new persistence; (d) FLAG-6 A Replay/Reconcile actions are thin delegators to ReplayService (T/S) + ReconciliationService (U), no second repair path, OPSC-S4 DoD needs a write-spy proof; (e) FLAG-7 A Flush Queue **removed**; (f) FLAG-8 C-modified **no Restart Workers** — status/heartbeat/runbook links only, supervisor owns lifecycle; (g) FLAG-10 A providers reuse the delivery `DatabaseConnectionInterface` (K), four-handle topology (L Ruling 0) unchanged, no new `pg_*` wrapper (E); (h) FLAG-11 A operations contracts under `core/Contracts/Operations/` (Rule 5 verbatim); (i) FLAG-2 A `core/Operations/` (lowercase) added as core infrastructure; (j) philosophy clause — console is observability/diagnostics only, **not** a control plane; infra orchestration permanently out of scope. **FLAG-9 B:** ratified ADR-047/048/049/050/052/053; **ADR-051 HELD** (not citable) pending FLAG-7/8 incorporation. Implications table gained seven DECISION V rows. **Doc 11:** added "Phase 1A – Expanded — Operations Console & Developer Experience" between Phase 1A and Phase 1B (§4 phase list + new §6b). **Doc 12:** status Draft → "Accepted (as amended by DECISION V)"; §21 self-freeze sentence removed; no other content edits. (Note: the file was already on disk as `docs/12-admin-operations-console-architecture.md` — the DoD rename was a no-op; no stale `(2)` references outside the audit note's historical record.) **CLAUDE.md:** folder structure gained `core/Operations/` + `core/Contracts/Operations/`; "Coding Standard TBD" replaced with the FLAG-4 A ruling; SETTLED gained a DECISION V line. **IMPLEMENTATION_PLAN.md §5b:** inserted OPSC-S0..S4 rows (all BLOCKED markers gone; OPSC-S3 server-rendered PHP, no `resources/` build config / no React/Assets; OPSC-S4 = Replay + Reconcile only, worker status/runbook read-only). **DOC12-ADOPTION-AUDIT.md:** FLAG-PLANOPS1-1..11 marked RESOLVED. Docs-only — no production code, no migrations, no tests touched.)
**Last updated:** 2026-07-15 (OPSC-S1 Operations Console core scaffolding **SHIPPED** — read-only, no UI, no actions; DECISION V (a)/(h)/(i); ADR-047/048/052/053. Pre-flight PASS: verified the six ratified ADR bodies (047/048/049/050/052/053) exist and are consistent with DECISION V clauses (a)-(j); ADR-051 confirmed HELD and NOT cited. **New contracts (`core/Contracts/Operations/`, depend on nothing outside `core/Contracts/` — Rule 5 verbatim):** five registry contracts (`PageRegistryInterface`, `NavigationRegistryInterface`, `WidgetRegistryInterface`, `ActionRegistryInterface`, `AssetRegistryInterface`) + their immutable descriptors (`ConsolePage`, `NavigationItem`, `ConsoleWidget`, `ConsoleAction`, `ConsoleAsset`); five provider contracts (`HealthProviderInterface`, `MetricsProviderInterface`, `WorkerStatusProviderInterface`, `QueueStatusProviderInterface`, `EndpointProviderInterface`) over a common `OperationsProviderInterface` (`key()`), + read-only DTOs (`HealthReport`, `MetricSample`, `WorkerStatus`, `QueueStatus`, `EndpointDescriptor`) + the `Severity` scale (ADR-049). **New infra (`core/Operations/`, lowercase — DECISION V (i)):** five explicit-registration registries under `Registries/` (no reflection/scanning; duplicate → `\LogicException`, unknown-key → `\RuntimeException`, position-then-registration ordering — mirrors EventRegistry/AdapterRegistry); `Services/ConsoleStateStore` (request-scope snapshot cache — zero persistence, zero PG), `Services/RefreshCoordinator` (centralizes provider refresh into the store so widgets never poll independently — one provider call per refresh regardless of consumer count; explicit non-reflective snapshot extraction via a match over the known provider interfaces; rejects providers lacking a known data interface), and `Services/OperationsService` (the single UI-facing seam — ADR-053; constructed from core contracts + core services only, NO infrastructure class reachable from a would-be UI caller). **Wiring:** new `OperationsServiceProvider` (all singletons, constructor injection — ADR-012/Rule 7) registered in `ContainerBuilder` before `ContentServiceProvider`; opens NO PG handle, introduces NO `pg_*` wrapper, adds NO fifth handle (DECISION L Ruling 0 topology unchanged). **Tests:** 55 new Unit (five registry tests incl. duplicate + unknown-key + ordering + empty-field validation; `Severity`; `ConsoleStateStore`; `RefreshCoordinator` incl. one-call-per-refresh + snapshot-routing for all five provider kinds + bare-provider rejection; `OperationsService` incl. two-widgets-over-one-provider = one provider call + widget→providerKey resolution + no-infra-in-ctor; container-resolution smoke proving every binding resolves via constructor injection as singletons). Full Unit suite **862/862 green, 0 failures, 0 deprecations**, deterministic on two consecutive runs (was 807 + 55). Verified: no concrete Health/Metrics/Worker/Queue/Endpoint provider (OPSC-S2), no UI/admin/wp-admin code (OPSC-S3), no operational-action behaviour (OPSC-S4), no migrations, no module-side code, no PG reads; `git status` shows only in-scope paths. No new flags.)
**Last updated:** 2026-07-15 (OPSC-S2 Diagnostics + Metrics providers **SHIPPED** — current-state, derived-only; DECISION V (c)/(g)/(h); DECISION P/Q; ADR-049; OPEN-8. **New core reader (`core/Operations/Diagnostics/OperationsQueryReader`):** the single delivery-handle read seam — every method a SELECT over the EXISTING frozen tables (`system.queue_jobs`, `system.dead_letter_jobs`, `system.worker_heartbeats`, `system.events`, `system.aggregate_versions`, `system.module_versions`, `system.schema_versions`), zero DML. Reuses the delivery `DatabaseConnectionInterface` (DECISION K / V (g)) — **no fifth handle** (DECISION L Ruling 0 topology frozen at four), **no new `pg_*` wrapper** (DECISION E). **Four core providers (`core/Operations/Providers/`):** `QueueStatusProvider` (live queue depth + DLQ depth + oldest-pending age), `WorkerStatusProvider` (DECISION P — worker-offline is a `last_heartbeat_at`-age query at read time against a config threshold; read-only, no lifecycle — DECISION V (f)), `MetricsProvider` (derived-on-demand samples: queue/DLQ/worker counts + processing-rate over a config window + replay pending/completed + reconciliation backlog — DECISION V (c), zero persistence), `HealthProvider` (ADR-049 severity rollup incl. DB-unreachable → CRITICAL). **System Information (`SystemInformation`+`SystemInformationProvider`, Doc 12 §13):** platform/PHP/WP/PG versions + queue provider + migration state + module versions (OPEN-8 reads). **Module Inspector (Doc 12 §14):** new **additive** core contract `ModuleInspectionProviderInterface` + immutable `ModuleInspection` DTO under `core/Contracts/Operations/` (architect ruling 2026-07-15, Option A) — descriptive metadata only, no query on the inspection path; deliberately **NOT** an `OperationsProviderInterface`, so it stays OFF the RefreshCoordinator snapshot path and the coordinator's five-kind match is untouched (constraint 4 → "directly-queried diagnostics service" chosen). Core `ModuleInspector` aggregator holds module descriptors by explicit injection. **Module-provided ops (`modules/Content/Operations/`, Rule 5 — depend on `core/Contracts/Operations/` only):** `ContentModuleInspection` (self-description, version from module.json), `ContentEndpointProvider` (the six `hsp/v1` routes behind `EndpointProviderInterface`), `ContentMetricsProvider` (live `content.*` projection row counts on the delivery handle). **Wiring:** OperationsServiceProvider gains the reader + four core providers + System Info + ModuleInspector, and registers the four core providers with the RefreshCoordinator in `boot()`; ContentServiceProvider registers its metrics/endpoint providers + module descriptor in `boot()`. Config: `worker.php` gains `heartbeat.offline_after_seconds` (60) + `console.processing_rate_window_seconds` (300) — config-driven, no schema, no persistence. **Tests:** 41 new Unit (readers/providers over a scripted read-only fake that throws on any write; wiring smoke proving the four core keys register with the coordinator) + 9 live-PG Integration proving live depths/DLQ/oldest-pending, heartbeat-age online/offline, derived point-in-time metrics, OPEN-8 version reads, Content projection counts, **four-handle topology unchanged (relay/queue/delivery/dispatcher = four DISTINCT backend PIDs, provider reads on the delivery PID, no fifth), and zero DML via a write-spy over every provider**. Full Unit **903/903 green, 0 failures, 0 warnings, 0 deprecations** (was 862 + 41), deterministic on two runs; OPSC-S2 Integration 9/9 green on two runs; full Integration suite 131 pass / 37 env-skipped / 0 failures **except** GATE-S4 criterion-1's `git status --porcelain -- core/` guard, which fails only because this session's in-scope `core/` files are uncommitted — it clears on commit (see FLAG-OPSCS2-1). Verified: no OPSC-S1 contract/registry/service file modified (only two additive contract files added); no UI/wp-admin (OPSC-S3), no action behaviour (OPSC-S4), no migration, no new table; `git status` shows only in-scope paths. **Module Inspector seam:** `ModuleInspectionProviderInterface` (additive) + kept OUT of the refresh/snapshot path — OPSC-S3 consumes it as a directly-queried diagnostics service.)
**Last updated:** 2026-07-15 (OPSC-S3 Operations Console UI + API Playground **SHIPPED** — server-rendered PHP + minimal vanilla JS, no node/npm/bundler; DECISION V (a)/(b); ADR-047/050/052/053; DECISION N/F. **New pure renderers (`core/Operations/UI/`, WordPress-free, unit-testable):** `Html` (output-escaping helper — prefers WP `esc_*`, falls back to `htmlspecialchars` so the escape-at-output guarantee holds in tests; DECISION V (b)); `DashboardView` (renders one read-only widget per snapshot shape — QueueStatus / HealthReport[] / MetricSample[] / WorkerStatus[] — plus System Information and Module Inspector; every dynamic value escaped through `Html`); `PlaygroundView` (Endpoint Explorer + Request Builder + Response Viewer over `EndpointDescriptor` metadata — no hardcoded routes, ADR-050/052). **New wp-admin boundary (`core/Operations/Admin/`):** `AdminPageController` (registers the two MVP menu pages — Operations + API Playground — with per-page capabilities discovered from `ConsolePage` descriptors; renders both server-side; enqueues static assets + localizes the nonce on `admin_enqueue_scripts`; guards every render with `current_user_can`/`wp_die`); `ConsoleAjaxController` (two nonce+capability-protected admin-ajax endpoints — poll returns serialized provider snapshots, execute runs one live GET — each verifying nonce via `check_ajax_referer`, checking capability, and sanitizing every input at the boundary; DECISION V (b)); `PlaygroundRequestExecutor` (live GET via in-process `rest_do_request` against the published `hsp/v1` contract — ADR-050/Rule 6; endpoint selected by INDEX into the metadata so the client can't target an arbitrary path; GET-only guard — read-only console); `ConsoleAdminRegistrar` (thin hook registrar — `admin_menu` / `admin_enqueue_scripts` / `wp_ajax_*`; no-op outside WP). **ADR-053 seam enforced mechanically:** a reflection test asserts no UI/Admin constructor receives a `DatabaseConnectionInterface`, the `OperationsQueryReader`, or any concrete OPSC-S2 provider — the UI reads exclusively through `OperationsService` (widgets/snapshots + `endpointDescriptors()`) and the two directly-queried diagnostics services (`SystemInformationProvider`, `ModuleInspector`). **Static assets (`resources/operations/console.{js,css}`):** hand-authored, self-contained — minimal vanilla JS does polling only (dashboard refresh + Playground execute); no bundle, no build step, no `resources/` build config (DECISION V (a)). **Wiring:** `OperationsServiceProvider` gains the UI/Admin bindings (register()) and registers the two console pages, MVP nav, four core dashboard widgets, and the static assets (boot()); `ContentServiceProvider::boot()` registers a module-owned Content-Projections widget over `ContentMetricsProvider::KEY` (Rule 5); `headless-sync.php` binds `ConsoleAdminRegistrar` in a normal admin request. **Additive service methods (no OPSC-S1/S2 contract/registry/provider/reader changed):** `OperationsService::endpointDescriptors()` + `RefreshCoordinator::endpointKeys()` aggregate endpoint metadata for the Playground. **Tests:** 37 new Unit (Html escaping; DashboardView per-shape + hostile-value escaping + system/module rendering; PlaygroundView explorer/builder/escaping; PlaygroundRequestExecutor path-build/{slug}/GET-only/out-of-range; ConsoleAjaxController nonce+capability+sanitize+route-through-OperationsService; AdminPageController menu-caps/server-render/capability-deny/asset-enqueue; ConsoleUiWiring pages/nav/widgets/assets resolve; NoInfrastructureReachableFromUi reflection guard). Full Unit **940/940 green, 0 failures, 0 warnings, 0 deprecations** (was 903 + 37), deterministic on two consecutive runs; full Integration 0 failures with DB-dependent tests env-skipped (GATE-S4 git-clean guard skips without live PG — clears on commit per FLAG-OPSCS2-1). End-to-end render verified: both pages render fully server-side with real snapshots, Playground lists all six `hsp/v1` endpoints and dispatches a live GET to `/hsp/v1/posts`. Verified: no state-changing action anywhere in the UI (actions are OPSC-S4); no infra reachable from UI code; no migration/schema/new PG handle; `git status` shows only in-scope paths. No new flags.)
**Last updated:** 2026-07-16 (OPSC-S4 Operational Actions **SHIPPED** — Replay + Reconcile ONLY, thin delegators; DECISION V (d)/(e)/(f); DECISION T/S/U; ADR-053 (ADR-051 HELD — not cited). **New core action seam (`core/Operations/Services/OperationsActionService`):** the action-side mirror of `OperationsService` — a THIN DELEGATOR holding ONLY the two ratified worker strategies (`ReplayWorkerStrategy` → `ReplayService` DECISION T entity/range; `ReconciliationWorkerStrategy` → `ReconciliationService` DECISION U drift/incremental/full) + a `StructuredLogger`. It reaches NO `DatabaseConnectionInterface`/adapter/reader, so the **write-spy proof holds by construction** — no write primitive is reachable; repair is re-emission only (never a second repair path). `execute($key,$params)` routes replay entity/range + reconcile drift/incremental/full (+ dry-run); the only permitted keys are `replay`/`reconcile` — Flush Queue / Restart Workers / any other key is rejected. **New DTO (`core/Contracts/Operations/ActionResult`):** immutable action outcome (action/ok/summary/count/detail), plain JSON-friendly — no infra handle. **New wp-admin action boundary (`core/Operations/Admin/ConsoleActionController`):** ONE nonce-protected admin-ajax action (`hsp_ops_action`) that, at the WP boundary (DECISION V (b)): verifies nonce (`check_ajax_referer`), resolves the target `ConsoleAction` via `OperationsService` (capability + confirmation policy **discovered from the descriptor**, never hardcoded), enforces `current_user_can` + **confirmation** (descriptor `confirmationRequired`), sanitizes every input, then delegates THROUGH `OperationsActionService`. Talks only to `OperationsService` + `OperationsActionService` — no infra (ADR-053; added to the reflection guard). **Audit** = one `operations.action` structured-log line per executed action via the EXISTING `StructuredLogger` observability path (DECISION Q) — **zero new persistence, no table, no schema** (DECISION V (c)). **Wiring:** `OperationsServiceProvider` binds the service + controller (strategies resolved lazily — `WorkerServiceProvider` registers first, so order is safe), registers the ConsoleAdminRegistrar's new action hook, and registers EXACTLY two `ConsoleAction`s (Replay + Reconcile, both capability-gated + confirmation-required); **no Flush Queue (V (e)), no Restart Workers (V (f))** action registered. Worker status/heartbeat/runbook links remain the read-only S2/S3 surface (untouched). **Carryover fix (OPSC-S3 nit):** `PlaygroundRequestExecutor` now selects the endpoint by a **stable route key** (`METHOD /namespace/route`, `keyFor()`) instead of a positional index — `PlaygroundView` emits the key as the `<option>` value, `ConsoleAjaxController` passes the sanitized key, `console.js` posts it; a registration-order shift between render and execute now resolves to the SAME endpoint (proven by a new order-shift test). **Tests:** 24 new Unit (OperationsActionService delegation for all replay/reconcile modes + dry-run + audit-line + write-spy zero-direct-writes over the REAL services + unknown/flush/restart rejection + param validation; ConsoleActionController nonce/capability/**confirmation**/sanitize/route-through + invalid-params→400; ConsoleActionWiring: seam resolves, exactly `[reconcile, replay]` registered, **no flush/restart under any key** asserted mechanically; Playground route-key + order-shift-stability) + 3 new live-MySQL+PG Integration (`tests/Integration/Operations/OperationsActionIntegrationTest`): replay-entity action converges a corrupt projection through the seam with `WriteSpyConnection.executeCount == 0`; reconcile-drift action repairs a missed capture through the seam, zero direct writes; executed action emits one structured audit line. Full **Unit 964/964 green, 0 failures, 0 deprecations** (was 940 + 24), deterministic on two consecutive runs; OPSC-S4 + related Integration 20/20 green on two runs; full Integration 136/137 pass — the sole failure is GATE-S4 criterion-1's `git status --porcelain -- core/` guard, red only because this session's in-scope `core/` files are uncommitted (identical to FLAG-OPSCS2-1; clears on commit). Verified: no OPSC-S1/S2/S3 contract/registry/provider changed except additive (`ActionResult` added; `PlaygroundRequestExecutor` signature updated for the nit); no migration, no new table, no new PG handle (four-handle topology unchanged — DECISION L Ruling 0), no new `pg_*` wrapper (DECISION E); `git status` shows only in-scope paths. No new flags.)
**Last updated:** 2026-07-16 (ONB-S0 DECISION W Ratification **SHIPPED** — docs-only; DECISION W (Onboarding & First-Run Backfill); OPSC-S0 pattern. Adopted the Onboarding / First-Run feature into the frozen record. **ARCHITECTURE_DECISIONS.md v1.20→v1.21:** added DECISION W recording six architect rulings (2026-07-16) — **(a) UI stack: DECISION V (a) AMENDED (globally) — React + shadcn adopted as THE admin UI stack; build-artifact policy = commit `dist/` to the repo (npm build in dev/CI only, production deploy = file copy, no host build step); WPCS security extended to the REST/ajax endpoints the React app calls; the shipped OPSC-S1..S4 server-rendered PHP console is NOT rewritten, only new admin UI (incl. onboarding) is React; the DECISION V provider/registry architecture is unchanged**; (b) backfill = full-reconciliation re-emission via `ReconciliationService::reconcileFull()` (DECISION U) through the normal outbox→relay→dispatch→worker pipeline — NO direct WP→PG copy, no second repair path, write-spy proof in the ONB-S2 DoD; (c) queue drain = **live worker heartbeat is a hard prerequisite** (DECISION P age check), workers drain as normal, no in-request tick drain; (d) progress derived on-demand (DECISION Q), completion = single WP option `hsp_onboarding_state` in MySQL, zero new persistence / no schema change; (e) placement = `core/Onboarding/` (NOT `core/Operations/` — keeps DECISION V (j) console-is-observability-only), delegates to ratified services only, no PG handle of its own (DECISION K reuse; L Ruling 0; E); (f) nav gating — Operations + API Playground hidden until `hsp_onboarding_state = complete`, five preflight checks (pgsql ext, PG constants, PG reachable, migration-engine state, PHP version) hard-block progression. **DECISION V (a) annotated as amended** (pointer to DECISION W added; the two do not silently contradict). Implications table gained seven DECISION W rows. **IMPLEMENTATION_PLAN.md §5b:** inserted ONB-S1 (preflight + onboarding page + nav gating + completion flag) and ONB-S2 (backfill trigger + derived live progress + redirect to Operations) rows with scope/authority/DoD/depends-on, sequenced after OPSC-S4 and before Phase 1B; the §5b closing note updated to record the DECISION V (a) amendment as explicit/ratified (not a silent conflict). **CLAUDE.md:** folder structure gained `core/Onboarding/` + reconciled the `core/Operations/` and `resources/` notes to the React amendment; Coding Standard gained the DECISION W (a) admin-UI-stack + WPCS-at-ajax-boundary note; SETTLED gained a DECISION W line and annotated the DECISION V UI clause as superseded. Docs-only — no production code, no migrations, no tests touched. **One acknowledged amendment (NOT a silent conflict):** DECISION W (a) amends frozen DECISION V (a); the architect explicitly chose the amendment path (see flags below).)
**Last updated:** 2026-07-16 (ONB-S1a Frontend toolchain bootstrap + onboarding page skeleton **SHIPPED** — React/shadcn toolchain + mount seam only; DECISION W (a)/(e); DECISION V (b)/(j); DECISION K/L Ruling 0/E; ADR-012; Rule 5. **STEP 0 (docs, architect-ruled 2026-07-16):** split the §5b ONB-S1 row into **ONB-S1a** (this session — toolchain + page skeleton) and **ONB-S1b** (next — five preflight checks + nav gating + `hsp_onboarding_state` flag + tests, carrying the remaining original DoD verbatim; depends-on ONB-S1a); ONB-S2 depends-on flipped to ONB-S1b; §5b closing note updated to ONB-S1a/S1b/S2. Appended a **styling addendum under DECISION W (a)**: dark theme is the DEFAULT for the React/shadcn admin stack (shadcn `.dark` token strategy), applied on the plugin mount root only — never the wp-admin `<body>`; the shipped OPSC console keeps its styling until a future console-migration session. **Frontend toolchain (`resources/admin-ui/`, npm):** `package.json` + `package-lock.json`; **Vite 5 library/IIFE build** → committed `dist/hsp-onboarding.{js,css}` with **stable, non-hashed filenames** (deterministic PHP enqueue, no manifest); React 18 bundled in (self-contained — no CDN, no host build step); Tailwind 3 + shadcn tokens with **preflight DISABLED** and every utility scoped via `important: '.hsp-admin-ui'` + a mount-scoped base layer, so wp-admin outside the mount is untouched (verified: the only global CSS rule sets `--tw-*` variables only — zero visual resets); `src/` (main.tsx mount seam applies `.hsp-admin-ui dark` on the mount root only; App.tsx skeleton; Card/Button primitives; bootstrap.ts reads the localized nonce/REST base). `npm run build` produces `dist/` (498 kB js / 7 kB css). **PHP registrar (`core/Onboarding/`):** `OnboardingPageController` registers ONE capability-gated "HSP Onboarding" wp-admin page (`add_menu_page`), renders a server-side shell carrying the single `#hsp-admin-ui-root` mount element, enqueues the committed `dist/` assets + localizes `HSP_ONBOARDING` (nonce + REST base + version) for the ONB-S1b endpoints; output-escaped + capability-guarded at the WP boundary (DECISION V (b)). `OnboardingAdminRegistrar` = thin hook registrar (`admin_menu` + `admin_enqueue_scripts`; no-op without WP). Placed in `core/Onboarding/` NOT `core/Operations/` (DECISION W (e); console unaffected — V (j)). **Wiring:** new `OnboardingServiceProvider` (both singletons, constructor injection — ADR-012) registered in `ContainerBuilder` after OperationsServiceProvider; `headless-sync.php` binds the registrar on `plugins_loaded` (priority 20). **`.gitignore`:** `node_modules/` ignored everywhere; `!headless-sync/resources/admin-ui/dist/` guard so the committed bundle is tracked (verified via `git check-ignore`). **CLAUDE.md:** frontend build-command rows replaced the TBD hold with the real `npm install` / `npm run build` / `npm run dev` commands, cited to DECISION W (a). **Tests:** 8 new Unit (`OnboardingPageControllerTest`: single capability-gated page, custom-capability, React mount-shell render, capability-deny → wp_die, committed-dist enqueue + `HSP_ONBOARDING` bootstrap localization, no-enqueue on unrelated pages; `OnboardingWiringTest`: bindings resolve as singletons via constructor injection + registrar `register()` safe no-op) + a `rest_url` WP stub added to `tests/bootstrap.php`. Full **Unit 973/973 green, 0 failures, 0 deprecations** (was 965 + 8); full suite 1110 tests, 0 failures, 121 DB-env-skipped (GATE-S4 `git status` guard skips without live PG — clears on commit per FLAG-OPSCS2-1). Verified: **NO PG handle opened, NO new `pg_*` wrapper, NO schema change, NO OPSC file modified**; `git status` shows only in-scope paths. No new flags.)
**Next session:** ONB-S1b — five preflight checks (pgsql ext, PG constants, PG reachable, migration-engine state, PHP version, all hard-blocking) + nav gating (hide Operations + API Playground until `hsp_onboarding_state = complete`) + the `hsp_onboarding_state` WP option + unit tests. See `docs/IMPLEMENTATION_PLAN.md` §5b (ONB-S1b row) and DECISION W. Depends-on ONB-S1a (done). Phase 1B remains post-MVP and is sequenced after ONB-S2.

---

## Session Checklist

### Phase 0 — Foundation

- [x] P0-S1 Bootstrap + DI container + configuration system
- [x] P0-S2 Migration engine
- [x] P0-S3 Module registry / discovery / lifecycle
- [x] P0-S4 Outbox capture + RelayWorkerStrategy
- [x] P0-S5 DB queue provider
- [x] P0-S6 Worker engine + strategies + event/adapter registries
- [x] P0-S7 Phase 0 DoD gate verification

### Phase 1A — Blog MVP

- [x] P1A-S1 Content events + WP hook wiring + EventProvider
- [x] P1A-S2 Extractors + source models + validators
- [x] P1A-S3 Transformers + canonical models
- [x] P1A-S4 Content migrations + PostgreSQL adapters
- [x] P1A-S5 REST Delivery API
- [x] P1A-S6a Bootstrap/DI fix — module boot + REST routes
- [x] P1A-S6b Content Subscriber/Handler spine
- [x] P1A-S6c Delivery connection isolation (DECISION K)
- [x] P1A-S6d Dispatcher stage (system.events → system.queue_jobs, DECISION L)
- [x] P1A-S6 Next.js validation + end-to-end DoD
- [x] P1A-S7 REST namespace rename: api/v1 → hsp/v1 (DECISION N)
- [x] P1A-S8 Env → define config resolution

### Early Operational Baseline

- [x] OPS-S1 Early Operational Baseline (DLQ inspect/replay, worker health, metrics)
- [x] OPS-S2 Replay Engine — entity + date-range replay (DECISION T — synthetic re-emission; FLAG-OPSS2-1 resolved)
- [x] OPS-S3 Reconciliation MVP — drift detection + WordPress-wins repair via DECISION T re-emission (DECISION U; FLAG-GATES3-1 resolved)

### Phase 1A – Expanded — Operations Console & Developer Experience

- [x] OPSC-S0 Doc 12 Ratification — DECISION V (v1.20); ADR-047/048/049/050/052/053 ratified, ADR-051 HELD (docs-only)
- [x] OPSC-S1 Operations Console core scaffolding — registries + provider contracts (`core/Contracts/Operations/`) + `core/Operations/` services; read-only, no UI, no actions (DECISION V (a)/(h)/(i))
- [x] OPSC-S2 Diagnostics + Metrics providers (current-state, derived-only per DECISION Q) — Health/Worker/Queue/Metrics core providers + System Information + Module Inspector; delivery-handle reads, no fifth PG handle, zero DML (write-spy proven)
- [x] OPSC-S3 Operations Console UI (server-rendered PHP) + API Playground — wp-admin Operations dashboard (read-only widgets over OPSC-S2 providers via OperationsService only, ADR-053) + API Playground over the six `hsp/v1` endpoints from EndpointProviderInterface metadata; escaping/sanitization/capability/nonce at the admin boundary (DECISION V (a)/(b)); no state-changing UI
- [x] OPSC-S4 Operational Actions — Replay + Reconcile only (thin delegators; write-spy proof)

### Onboarding & First-Run (DECISION W)

- [x] ONB-S0 DECISION W Ratification — Onboarding & First-Run adopted into the frozen record; DECISION V (a) amended (React+shadcn admin UI); ONB-S1/S2 inserted into §5b (docs-only)
- [x] ONB-S1a Frontend toolchain bootstrap + onboarding page skeleton (React+shadcn under `resources/admin-ui/`, committed `dist/`, dark-default scoped to mount root; `core/Onboarding/` registrar mounts the React shell — no preflight/gating/flag yet)
- [ ] ONB-S1b Five preflight checks + nav gating + `hsp_onboarding_state` completion flag (no backfill yet) — depends on ONB-S1a
- [ ] ONB-S2 Backfill trigger (full-reconciliation re-emission, DECISION U) + worker-heartbeat hard prerequisite + derived live progress (DECISION Q) + redirect to Operations

---

## Architecture Validation Gate

- [x] Reliability validation — **PASS** (GATE-S1: criteria 1–3 all PASS; criterion 2 re-run complete under DECISION T; FLAG-GATES1-1 RESOLVED)
- [x] Scalability validation — **PASS** (GATE-S2: criteria 1–3 all PASS; SKIP LOCKED verified with genuinely concurrent PG sessions; no new flags)
- [x] Operability validation — **PASS** (GATE-S3: criteria 1–3 all PASS with real gate evidence on live MySQL + PG; worker health visible + crash detected within one heartbeat cycle (DECISION P); DLQ payload snapshot + stack trace (OPEN-3); reconciliation executes in all three modes with WordPress-wins repair via DECISION T re-emission only, zero direct PG writes proven by write-spy. FLAG-GATES3-1 RESOLVED. No new flags.)
- [x] Extensibility validation — **PASS** (GATE-S4: criteria 1–3 all PASS with real gate evidence on live PostgreSQL; each test proves the positive half against the real runtime and asserts the negative half via a `git status --porcelain` guarded-path check with proven teeth. New content field rides the existing `CanonicalPost.meta` seam through the real `PostAdapter` (zero `core/` edits); new projection column derived from the unmodified real `PostTransformer`/`CanonicalPost` (zero transformer/canonical edits); new `/authors` resource rides the existing `QueryProviderInterface`/`ResourceInterface` (zero existing-endpoint edits). No new flags.)

Gate failure blocks Phase 2 and all subsequent phases. **The gate is now COMPLETE — all four sub-gates PASS.**

### GATE-S1 — Reliability Validation checklist (evidence: `tests/Integration/Gate/ReliabilityValidationTest.php`)

| # | §4 Reliability criterion | Result | Evidence (named integration test) |
|---|---|---|---|
| 1 | Successful sync processing under normal load | **PASS** | `test_criterion1_full_pipeline_syncs_a_batch_end_to_end_under_normal_load` (12-aggregate mixed batch: outbox → relay → dispatch → worker → `content.*` projection, live MySQL + PG); `test_criterion1_update_event_reprojects_the_aggregate_end_to_end` |
| 2 | Replay succeeds for single **event, entity, and date-range** replay modes | **PASS** | Three gate tests, all live MySQL + PG: `test_criterion2_single_event_replay_succeeds` (DECISION S single-DLQ-id entry point; exercised end-to-end by criterion 3); `test_criterion2_entity_replay_converges_a_corrupt_projection_to_correct_final_state` (corrupt slug+checksum → `hsp replay entity`/ReplayService → projection converges to current WP state, no duplicate row); `test_criterion2_date_range_replay_converges_in_window_aggregates_and_leaves_out_of_window_untouched` (two in-window aggregates corrupted → `replayRange` → both converge; out-of-window aggregate untouched, version not advanced; half-open [from,to)). Shipped DECISION T path (OPS-S2); full mode coverage also in `tests/Integration/Replay/ReplayEngineIntegrationTest`. |
| 3 | DLQ recovery: failed job replays to correct final state | **PASS** | `test_criterion3_exhausted_job_dead_letters_then_replays_to_correct_final_state` (retry-limit exhaustion → `system.dead_letter_jobs` with full OPEN-3 context → `hsp dlq replay` → correct final projection state → `replayed_at` stamped; live PG) |

GATE-S1 DoD is **met**: criteria 1–3 all PASS; the gate suite is 6/6 green with zero Incomplete markers; the full suite is 888 tests green, 0 failures. The Reliability item is marked done. FLAG-GATES1-1 is RESOLVED (DECISION T shipped the entity + date-range modes in OPS-S2; the criterion-2 re-run in this GATE-S1 session replaced the STOP-and-flag with real gate evidence). GATE-S2 (Scalability) is now cleared to begin.

### GATE-S2 — Scalability Validation checklist (evidence: `tests/Integration/Gate/ScalabilityValidationTest.php`)

| # | §4 Scalability criterion | Result | Evidence (named integration test) |
|---|---|---|---|
| 1 | Multiple concurrent worker processes claim jobs without collision (SKIP LOCKED verified) | **PASS** | Two live-PG tests: `test_criterion1a_concurrent_sessions_skip_locked_rows_without_blocking_or_double_claiming` — two independent PG sessions (physical links, `PGSQL_CONNECT_FORCE_NEW`) each hold a `FOR UPDATE` lock on a queue row at the SAME instant; session B runs the exact OPEN-4 claim SELECT while A holds its lock and gets a DIFFERENT row (no double-claim); B carries a 2 s `statement_timeout` so blocking on A's row would FAIL instead of hang (proves no claimant blocks); a third session skips BOTH locked rows (two claims genuinely concurrent, not serialized). `test_criterion1b_real_provider_claims_every_job_exactly_once_across_workers` — 12 jobs, 3 distinct `DatabaseQueueProvider` workers on 3 physical connections, round-robin `claim()`; every job claimed exactly once, claimed-set equals the whole queue, ≥2 workers participate, no PG-side double-claim. |
| 2 | Queue growth handled without head-blocking | **PASS** | `test_criterion2_stuck_head_job_does_not_block_other_ready_jobs` — worker A claims the HEAD job (earliest `available_at`) with a 10 h visibility timeout so it is stuck/held; worker B on its own connection claims and COMPLETES all 6 followers past the stuck head; B never claims the head; head stays `claimed` by A; only the head remains claimed. SKIP LOCKED skips the head's lock rather than waiting on it. |
| 3 | Replay under load does not corrupt normal processing | **PASS** | `test_criterion3_replay_interleaved_with_normal_processing_stays_correct` — replay (DECISION T `ReplayService::replayEntity`) fired for a corrupted aggregate group WHILE a normal-load batch (4 posts + 2 pages) is mid-flight in the same outbox → relay → dispatch → queue; all drained interleaved in one worker loop. Asserts: both replayed and normally-processed aggregates converge to correct final projection state; `aggregate_versions` strictly monotonic (replay advanced via fresh DECISION 2 counter, normal-load at v1, none regressed); one projection row per aggregate (no forks); zero duplicate `system.processed_events` (PK-keyed; distinct == total). |

GATE-S2 DoD is **met**: criteria 1–3 all PASS with real gate evidence (live MySQL + live PostgreSQL, no `markTestIncomplete`); the GATE-S2 suite is 4/4 green (65 assertions); the combined gate suite is 10/10 green (121 assertions) and deterministic on immediate back-to-back re-run (no cross-run residue); the full suite is 892 tests green, 0 failures (was 888 + 4 GATE-S2). Tests-only change — no production code touched. The Scalability item is marked done. No new flags. GATE-S3 (Operability) is now cleared to begin.

### GATE-S3 — Operability Validation checklist (evidence: `tests/Integration/Gate/OperabilityValidationTest.php`)

| # | §4 Operability criterion | Result | Evidence (named integration test) |
|---|---|---|---|
| 1 | Worker health visible; failure detection within one heartbeat cycle (DECISION P) | **PASS** | `test_criterion1_worker_health_is_visible_and_a_crash_is_detected_within_one_heartbeat_cycle` — the real `WorkerEngine.tick()` + `DatabaseHeartbeatPublisher` upsert one current-state row per worker into live `system.worker_heartbeats` (health VISIBLE: `worker_id`/`worker_type`/`status`, single row, no history — DECISION P); a monitor's `last_heartbeat_at`-age query flags a crashed worker (one that stopped ticking, beat older than one cycle) within one heartbeat cycle while leaving the freshly-ticked healthy worker unflagged; the derived `worker_count` metric (DECISION Q) reports both workers as visible. |
| 2 | Failure diagnostics via DLQ payload snapshot + stack trace (OPEN-3) | **PASS** | `test_criterion2_dead_lettered_job_carries_payload_snapshot_and_stack_trace` — a job driven to ADR-022 retry-limit exhaustion through the REAL worker path (`EventWorkerStrategy`) dead-letters into `system.dead_letter_jobs`; read back via the same operator surface (`DeadLetterRepository::inspect` / `hsp dlq inspect`), the row carries a non-empty real PHP `stack_trace` (frame markers present) and a NOT-NULL structured JSONB `payload_snapshot` (DECISION A — self-contained/replayable), plus the failure_reason; no projection written for the failed job. Live MySQL (relay) + live PG. |
| 3 | Reconciliation executes: hourly drift, incremental validation, full reconciliation (ADR-026); WordPress always wins divergence (ADR-027/ADR-045) | **PASS** | Four gate tests, all live MySQL + PG, repair via DECISION T re-emission ONLY (write-spy proves zero direct PG projection writes / zero write-txns on every mode). `test_criterion3a_hourly_drift_detection_repairs_a_missed_capture_wordpress_wins` (hourly drift detects a DECISION 1 missed capture → re-emit → projection converges to current WP state). `test_criterion3b_incremental_validation_repairs_checksum_drift_wordpress_wins` (nightly incremental checksum recompute catches a category rename invisible to existence-only hourly drift — D2 — → repair). `test_criterion3c_full_reconciliation_tombstones_an_orphan_via_re_emission_wordpress_wins` (weekly full PG→WP orphan sweep re-emits `.deleted` → DECISION I soft-delete tombstone). `test_criterion3d_reconciliation_is_idempotent_and_suppresses_in_flight_captures` (a second pass over consistent state re-emits nothing; a relayed-not-processed aggregate is suppressed as IN-FLIGHT — DECISION U D4, not flagged as drift). WordPress-wins holds by construction — reconciliation never writes WP or PG projections directly. |

GATE-S3 DoD is **met**: criteria 1–3 all PASS with real gate evidence (live MySQL + live PostgreSQL, no `markTestIncomplete`, no `markTestSkipped` reached when DBs present); the GATE-S3 suite is 6/6 green (52 assertions); the combined gate suite is 16/16 green (173 assertions) and deterministic on two consecutive cold runs; the full suite is 929 tests green, 0 failures (was 923 + 6 GATE-S3), with the single pre-existing P0-S5 Unit-suite deprecation unchanged. Tests-only change — no production code touched. The Operability item is marked done. No new flags. GATE-S4 (Extensibility) is now cleared to begin.

### GATE-S4 — Extensibility Validation checklist (evidence: `tests/Integration/Gate/ExtensibilityValidationTest.php`)

Each criterion proves the **positive half** against the real runtime (live PostgreSQL) and mechanically asserts the **negative half** with `git status --porcelain -- <path>` — which reports both tracked modifications and new untracked files, so an empty result is evidence the guarded area was untouched. The guard was verified to have teeth (temporarily editing a `core/` file makes criterion 1 fail). All "additions" are test-scoped throwaways (test schemas / doubles); the extension **seam** is what is proven, not a Phase 1B feature.

| # | §4 Extensibility criterion | Result | Evidence (named integration test) |
|---|---|---|---|
| 1 | Add a new content field to a projection without modifying `core/` | **PASS** | `test_criterion1_a_new_content_field_reaches_the_projection_without_touching_core` — a module-scoped transformer adds a `reading_time` content field via the EXISTING `CanonicalPost.meta` extension point; the REAL `PostAdapter` persists it into live `content.posts.meta_jsonb` with zero core AND zero adapter edits; the field is read back from the projection. NEGATIVE half: asserts zero working-tree changes under `headless-sync/core/`. (Finding: the real `PostAdapter` type-checks for `CanonicalPost` specifically, so the true seam is the `meta` map on the existing canonical model, not a new canonical subclass — the test reflects this.) |
| 2 | Add a new projection column without modifying transformer or canonical model | **PASS** | `test_criterion2_a_new_projection_column_is_populated_from_the_unmodified_canonical_model` — the REAL, UNMODIFIED `PostTransformer` + `CanonicalPost` produce the canonical model (asserted via `::class` identity, no subclass); a test-scoped adapter derives a NEW real column `word_count` from it and writes a throwaway `content.posts_ext` projection on live PG; the new column is read back. NEGATIVE half: asserts zero working-tree changes to `PostTransformer.php` and `CanonicalPost.php`. |
| 3 | Add a new API resource without modifying existing endpoints | **PASS** | `test_criterion3_a_new_api_resource_is_served_without_touching_existing_endpoints` — a new `AuthorQueryProvider` + `AuthorResource` implementing the EXISTING core `QueryProviderInterface`/`ResourceInterface` (asserted via `instanceof`) serve a live `content.authors` table: listing envelope (`data`/`next_cursor`), single-item `findBySlug`, and self-shaped contract fields. NEGATIVE half: asserts zero working-tree changes to `ContentRestRegistrar.php`, `Resources/`, and `Queries/`. |

GATE-S4 DoD is **met**: criteria 1–3 all PASS with real gate evidence (live PostgreSQL, no `markTestIncomplete`); the GATE-S4 suite is 3/3 green (28 assertions); the combined gate suite is 19/19 green (201 assertions) and deterministic on two consecutive cold runs; the full suite is 932 tests green, 0 failures (was 929 + 3 GATE-S4), with 0 deprecations. Tests-only change — no production code touched. The Extensibility item is marked done. **The Architecture Validation Gate is COMPLETE — all four sub-gates (Reliability, Scalability, Operability, Extensibility) PASS.** No new flags.

---

## Flags

### FLAG-OPSCS2-1 — GATE-S4 criterion 1 fails on a dirty tree while OPSC-S2 `core/` files are uncommitted (expected; clears on commit)

**Raised:** 2026-07-15 | **Session:** OPSC-S2 | **Status:** **RESOLVED 2026-07-15 (OPSC-S3 session close).** The resolution condition ("clears on commit") is met: OPSC-S3 committed all `core/Operations/` additions (commit `69d2d4a` on origin/main), and a clean-tree GATE-S4 rerun — `tests/Integration/Gate/ExtensibilityValidationTest.php` on live PG (127.0.0.1:5432) — is **3/3 green (28 assertions)**, so criterion 1's `git status --porcelain -- headless-sync/core/` porcelain guard is empty (green) again. Not a code defect; nothing further owed. **Original analysis retained below for context.**

GATE-S4 (`tests/Integration/Gate/ExtensibilityValidationTest`) criterion 1 asserts the working tree is clean under a guarded path via `git status --porcelain -- headless-sync/core/` (the "negative half" — proving a new content field required zero `core/` edits). OPSC-S2 legitimately and in-scope adds files under `headless-sync/core/Operations/` + `core/Contracts/Operations/` and modifies `core/Container/…` wiring. While those changes are **uncommitted**, the porcelain guard is non-empty and criterion 1 fails. This is a **snapshot-of-working-tree** property of that gate test, not a regression: the moment this session's changes are committed, `git status --porcelain -- core/` is empty again and GATE-S4 passes (as it did after every prior `core/`-touching session committed). Every other Integration test passes (131 pass / 37 env-skipped / 0 other failures); the full Unit suite is 903/903 green. **Resolution:** commit the OPSC-S2 changes (session close step 8). No production or gate-test change is warranted — the gate is asserting exactly what it should. Recorded so the mid-session red is not mistaken for a defect. (The same clears-on-commit property re-applied to OPSC-S3's own `core/Operations/{Admin,UI}/` additions; the step-3 clean-tree rerun above confirms it.)

---

### FLAG-GATES3-1 — GATE-S3 criterion 3 (Reconciliation) is unsatisfiable: reconciliation is a stub, never built

**Raised:** 2026-07-13 | **Session:** GATE-S3 (pre-implementation) | **Status:** **RESOLVED — DECISION U (ARCHITECTURE_DECISIONS.md v1.18, 2026-07-13); Option A ratified.** OPS-S3 "Reconciliation MVP" is inserted in the Session Map immediately before GATE-S3; GATE-S3 Depends-on changes to OPS-S3; GATE-S3 DoD is UNCHANGED. Reconciliation un-stubs `ReconciliationWorkerStrategy` and repairs delivery drift ONLY via the DECISION T re-emission primitive (`ReplayService`/`ReplayEmitterInterface`) — direct PG projection writes prohibited; WordPress-wins holds by construction (ADR-026/027/045). Scope: drift detection + incremental validation + full reconciliation, detecting (a) missed captures (WP newer/absent vs delivery — DECISION 1 backstop) and (b) orphans (present in delivery, deleted in WP → re-emit `.deleted` → DECISION I tombstone). Scheduling: WP-Cron authorized (recovery-jobs carve-out) to *trigger* only; workers remain the execution path. Design ratified in-chat (DECISION U v1.19 — three modes, D2 taxonomy existence-only, D4 suppression rule, D5 executor B1, D6 core contract, D7 unbounded paged full sweep) and **BUILT + tested in OPS-S3 (2026-07-13):** un-stubbed strategy façade over `ReconciliationService`; 25 unit + 6 live-DB integration tests green; full suite 923 green on two cold runs. GATE-S3 criterion 3 is now satisfiable. The original flag analysis is retained below for context.

**STOP condition hit before writing any gate test. No production code was changed and no gate test was written.** GATE-S3 (Operability Validation) has three §4 criteria. Criteria 1 (worker health / failure detection within one heartbeat cycle — DECISION P) and 2 (DLQ payload snapshot + stack trace — OPEN-3) are backed by real production code and are validatable. **Criterion 3 (Reconciliation) cannot be validated because the production behavior it must exercise does not exist.**

**The gap:** GATE-S3 criterion 3 requires: *"Reconciliation executes: hourly drift detection, incremental validation, and full reconciliation (ADR-026); on divergence, WordPress state always wins and PG is repaired — never the reverse (ADR-027, ADR-045)."* Against the current tree:

1. `ReconciliationWorkerStrategy::execute()` ([ReconciliationWorkerStrategy.php:23-27](headless-sync/core/Workers/Strategies/ReconciliationWorkerStrategy.php#L23-L27)) is a **STUB** — it returns `false` and performs no drift detection and no repair. Its own comment states domain logic is "implemented in OPS-S1 / Phase 3 Operational Hardening."
2. It is registered as a stub in the DI container ([WorkerServiceProvider.php:113](headless-sync/core/Container/Definitions/WorkerServiceProvider.php#L113), doc-comment line 36 labels it "stub").
3. A full-codebase search (`core/`, `modules/`) finds **no** drift-detection routine, **no** incremental validation, **no** full reconciliation, and **no** WordPress-wins repair path. There is **no** `hsp reconcile` CLI command (the `hsp` surface has DLQ + replay only), **no** scheduled/cron wiring, and **no** reconciliation service. ADR-026 (Doc 4 §886) requires a schedule — Hourly Drift Detection / Nightly Incremental Validation / Weekly Full Reconciliation — none of which is wired.

**Why this is a STOP, not a fix (per the GATE-S3 brief):** The brief states *"NO production code changes"* and *"If a validation check fails, that is a STOP-and-flag, not a fix."* Building reconciliation is production code and is out of GATE-S3 scope. The DoD also forbids `markTestIncomplete`. Writing gate evidence against absent behavior would require either improvising the reconciliation mechanism (prohibited — no DECISION specifies drift-detection scope, cadence driver, or repair granularity beyond the DECISION T primitive) or a placeholder test (disallowed). Neither is permitted.

**Sequencing conflict (root cause):** The IMPLEMENTATION_PLAN.md §5b Session Map places **GATE-S3** (which requires reconciliation to *execute*) before any session that *builds* reconciliation. OPS-S1's scope (Session Map line 380) shipped heartbeat + DLQ + replay + metrics — **not** reconciliation. Phase 3 "Operational Hardening" (IMPLEMENTATION_PLAN.md §5), where reconciliation is deferred, is post-MVP and unscheduled. The frozen docs corroborate reconciliation as future work: DECISION T ([ARCHITECTURE_DECISIONS.md:792](docs/ARCHITECTURE_DECISIONS.md#L792)) — *"...future reconciliation (ADR-026/ADR-027/ADR-045) will use... Reconciliation sessions build on DECISION T rather than inventing a second repair path."* No reconciliation session has been run. GATE-S3 criterion 3 is therefore unsatisfiable as sequenced.

**What is NOT in dispute:** Criteria 1 and 2 are ready to validate. This flag blocks only the reconciliation criterion — and, because the gate is all-or-nothing, GATE-S3 completion overall.

**Options (architect must choose ONE — none may be implemented without a DECISION):**

- **Option A — Insert a reconciliation build session before GATE-S3.** Add an `OPS-S3` (or `RECON-S1`) session that un-stubs `ReconciliationWorkerStrategy`: drift detection (compare WordPress source-of-truth against `content.*` projections), incremental validation, full reconciliation, and WordPress-wins repair — built on the DECISION T emit-through-outbox primitive (per ARCHITECTURE_DECISIONS.md line 792). Requires the architect to specify: drift-detection query/scope, the three cadences' scheduling driver (WP-Cron fallback vs. worker vs. external), and repair granularity. GATE-S3 then validates real behavior afterward. This is the path most consistent with the frozen "reconciliation sessions build on DECISION T" ruling.
- **Option B — Descope GATE-S3 criterion 3 to Phase 3.** Rule that Operability Validation for the Blog MVP gate covers criteria 1 + 2 only (heartbeat/failure detection + DLQ diagnostics), and that reconciliation validation moves to Phase 3 Operational Hardening where reconciliation is actually built. Amend IMPLEMENTATION_PLAN.md §4 Operability Validation and the GATE-S3 DoD accordingly. GATE-S3 then passes on the two implemented criteria.
- **Option C — Reduced-scope reconciliation for the gate.** Ratify a minimal reconciliation implementation sufficient for the gate (e.g. on-demand full reconciliation via a `hsp reconcile` command using DECISION T re-emission, deferring the hourly/nightly/weekly *scheduling* to Phase 3), and authorize it as an in-scope build. *Tension:* still requires a production build (currently out of GATE-S3 scope) and a DECISION defining the reduced surface.

**Resolution trigger:** Architect selects one option, records it as a DECISION in `docs/ARCHITECTURE_DECISIONS.md`, and authorizes any session/scope/schema change it entails. Only then can GATE-S3 proceed. Until then, GATE-S3 criteria 1 and 2 evidence may be written, but GATE-S3 as a whole remains BLOCKED and the Operability item stays unchecked.

---

### FLAG-OPSS2-1 — Entity + date-range replay vs. DECISION J stale guard: reprojection mechanism unspecified

**Raised:** 2026-07-12 | **Session:** OPS-S2 (pre-implementation) | **Status:** **RESOLVED — DECISION T (ARCHITECTURE_DECISIONS.md v1.17, 2026-07-12); Option A ratified and built in OPS-S2.**

**Resolution (architect ratified Option A → DECISION T):** Replay is **projection repair via synthetic re-emission**. Entity and date-range replay emit a NEW event through `wp_hsp_outbox` taking a NEW `aggregate_version` from `wp_hsp_aggregate_counters` (DECISION 2), flowing relay → dispatch → worker and passing the DECISION J stale guard **naturally** (new version > stored) — the guard is not weakened, bypassed, or made conditional. Historical `system.events` rows are never mutated or re-enqueued (Doc 5 §26 holds; replay appends). The emitter reads CURRENT WordPress state (ADR-044/ADR-045): exists+public → `.updated`; missing/non-public → `.deleted` (tombstones outage-window deletions). Date-range mode: `SELECT DISTINCT (aggregate_type, aggregate_id)` from `system.events` in `[from, to)` via the existing delivery handle (no fifth handle). Traceability: one `correlation_id` per run, `causation_id` per operation. No schema change; no new pg_* wrapper; no new PG handle. The IMPLEMENTATION_PLAN.md §5b "re-enqueue original event" wording is superseded for entity/date-range modes; single-event DLQ replay (DECISION S) is unchanged. Built in OPS-S2 (see Session Log).

**STOP condition hit at pre-implementation (retained for context). No replay code was written before the ruling.** OPS-S2 was scoped to un-stub `ReplayWorkerStrategy` and build entity + date-range replay (Doc 4 §24). Before writing code, the brief required determining how Doc 4 §24 specifies replay to interact with the DECISION J stale guard. It does not — the mechanism is unspecified across all frozen docs, and the three governing constraints are mutually incompatible for the already-processed case.

**The conflict (three frozen constraints that cannot all hold):**

1. **Replay re-enqueues the original event version** — IMPLEMENTATION_PLAN.md §5b line 284: *"Replay re-enqueues the original event version; does not mutate historical contracts (Doc 5 §26)."* Doc 5 §26: *"Replay must use original event versions… Replay must never mutate historical contracts."*
2. **DECISION J stale guard is binding and may not be weakened/bypassed** — `EventWorkerStrategy::isStale()` ([EventWorkerStrategy.php:205](headless-sync/core/Workers/Strategies/EventWorkerStrategy.php#L205)): `event.aggregate_version <= stored latest_processed_version → stale → ack, ZERO writes`. Per the brief, this guard *"may not be weakened, bypassed, or made conditional without a DECISION."*
3. **DoD criterion 1/2 requires the entity to reproject to correct final state.**

For an aggregate that was **already successfully processed**, `system.aggregate_versions.latest_processed_version` already equals the newest event's `aggregate_version`. Re-enqueuing any existing `event_id` for that aggregate → the DECISION J guard sees `version <= stored` → **acks with zero projection writes**. The entity does **not** reproject. Constraints 1 + 2 + 3 cannot simultaneously hold.

**Why DLQ replay (DECISION S / OPS-S1) does NOT resolve this:** DLQ replay works precisely because a dead-lettered event's aggregate was *never successfully processed* — its version is still **ahead** of `latest_processed_version`, so the guard lets it through (DECISION S clause (c) even documents the "already-current → zero writes" case as acceptable *for that path*). Entity/date-range replay targets **already-current** aggregates, which is the opposite situation: the guard suppresses them.

**Why Doc 5 §26 does not disambiguate:** §26 governs `event_version` (the contract/schema version — "v1 stays v1"), a field distinct from the per-aggregate `aggregate_version` monotonic counter (Doc 5 lines 262 vs 268). §26 forbids rewriting event schema versions and mutating historical event contracts; it says nothing about the `aggregate_version`↔stale-guard interaction. IMPLEMENTATION_PLAN.md line 284's "original event version" is itself ambiguous between the two readings, and neither reading specifies the reprojection mechanism.

**Options (architect must choose ONE — I have NOT picked one, and none may be implemented without a DECISION):**

- **Option A — Fresh synthetic event via the outbox with a new counter version.** Replay re-captures current WP state (ADR-044/DECISION H) into `wp_hsp_outbox`, allocating a new `aggregate_version` via `wp_hsp_aggregate_counters` (DECISION 2). The new version is `> latest_processed_version`, so it passes the guard naturally and reprojects. *Tension:* appears to violate constraint 1 ("re-enqueues the **original** event version") if that phrase binds `aggregate_version`; must confirm §26 only binds `event_version`. Honors Rule 3 (goes through the outbox) and ADR-044 (reload current state). Requires no schema change.
- **Option B — Guard bypass for replay.** A replay-originated job carries a flag that instructs `EventWorkerStrategy` to skip the Resolve-stage guard (Layer 2 GREATEST guard still holds). *Tension:* directly weakens/makes-conditional the DECISION J guard — explicitly prohibited by the brief without a DECISION; also risks a replay racing a live higher-version event.
- **Option C — Version-state reset.** Before re-enqueue, delete/lower the `system.aggregate_versions` row for the target aggregate(s) so the original event is no longer `<=` stored. *Tension:* mutates version state (arguably "mutates historical" state), interacts with the GREATEST guard and `system.processed_events`, and risks regression flagged by DoD criterion 4.

**Also unspecified (dependent on the above):** whether date-range replay enumerates affected aggregates from `system.events` by `created_at`/`source_updated_at` window, or from another source; and the WP-CLI surface shape (`hsp replay entity <type> <id>` / `hsp replay range <from> <to>` — extending the OPS-S1 `hsp` command surface per the brief).

**Schema note:** If the ratified mechanism requires any new table or column (e.g. a replay-marker column, a replay-batch table), that is a **separate frozen-schema change requiring its own DECISION** before implementation — per the brief's out-of-scope clause. Option A requires none; Options B/C may.

**Resolution trigger:** Architect ratifies the replay↔stale-guard mechanism (one of the options above or another), records it as a DECISION in `docs/ARCHITECTURE_DECISIONS.md`, and authorizes any schema change it entails. Only then may OPS-S2 build entity + date-range replay. GATE-S1 criterion 2 remains BLOCKED (FLAG-GATES1-1) until OPS-S2 ships under that ruling.

---

### FLAG-P0S1-1 — PSR-11 stubs bundled as local source files

**Raised:** 2026-06-22 | **Session:** P0-S1

P0-S1 scope forbids `require`/`require-dev` entries in `composer.json`. The Container
implements `Psr\Container\ContainerInterface` (PSR-11). To satisfy both constraints,
`Psr\Container` interfaces are bundled as three stub files under `core/Psr/Container/`,
mapped via an additional `psr-4` entry in `composer.json`.

**Impact:** When `psr/container` is added as a proper composer dependency in a later session,
the stub files and the `"Psr\\Container\\"` `psr-4` entry in `composer.json` must be removed
to avoid class redeclaration errors. This is a one-line composer require + delete of three
files + remove the composer.json psr-4 line.

**Resolution trigger:** This flag is resolved when the project introduces the official runtime
`psr/container` package via Composer. At that point: remove the temporary `core/Psr/Container/`
interfaces; remove the associated temporary PSR-4 mapping; refactor the Container to depend on
the official package; verify the platform boots and all tests pass. `require-dev` tooling
(e.g. phpunit) does NOT trip this trigger.

---

### FLAG-P0S3-1 — core/Module/ (singular) vs core/Modules/ (plural)

**Raised:** 2026-06-22 | **Session:** P0-S3

Doc 2 §10 uses `core/Modules/` (plural). IMPLEMENTATION_PLAN.md §5b P0-S3 and the session brief both specify `core/Module/` (singular). Per IMPLEMENTATION_PLAN.md §1, the session brief overrides Doc 2. Proceeded with `core/Module/` (singular) as specified in the authoritative session map.

**Resolution trigger:** If Doc 2 §10 is ever amended to match the session map, this flag can be closed. No code change needed — the ruling is already consistent with the operative authority.

---

### FLAG-P0S3-2 — phpunit/phpunit ^11.5 added to require-dev

**Raised:** 2026-06-22 | **Session:** P0-S3 | **Status:** Accepted

`phpunit/phpunit ^11.5` added to `require-dev` to run unit tests. Per the FLAG-P0S1-1 ruling (Intent), a dev-only tool does not trip the PSR-container resolution trigger. No runtime impact.

---

### FLAG-P0S4-1 — `'relaying'` intermediate outbox status not in frozen DDL ENUM

**Raised:** 2026-06-22 | **Session:** P0-S4 | **Status:** Resolved — P0-S4 follow-up (2026-06-22)

**Resolution:** Redesigned by removing the intermediate status entirely. `RelayWorkerStrategy` now holds the MySQL `FOR UPDATE` row lock for the entire batch duration: `BEGIN` → SELECT FOR UPDATE SKIP LOCKED → (PG insert + MySQL mark-`'relayed'` per row) → `COMMIT`. The row lock is the claim guard; no status transition to `'relaying'` is needed. OPEN-6 v1.3 frozen `ENUM('pending','relayed')` DDL is correct as-is — no migration change. 91/91 unit tests pass; a negative-assertion test (`test_tick_does_not_use_relaying_intermediate_status`) confirms `'relaying'` never appears in emitted SQL.

---

### FLAG-P0S4-2 — Integration tests self-skipped; live-DB DoD items unproven

**Raised:** 2026-06-22 | **Session:** P0-S4 follow-up | **Status:** Fully resolved — 2026-06-22

**Counter test (item 1): RESOLVED.** `ConcurrentAggregateVersionTest` ran against live MySQL (localhost:10053, db `local`) and passed 3/3. Bug found and fixed: `VALUES (%s, %s, 1)` → `VALUES (%s, %s, LAST_INSERT_ID(1))` so `LAST_INSERT_ID()` returns `1` on first insert.

**Relay end-to-end test (item 2): RESOLVED.** `RelayEndToEndTest` written and passed 5/5 against live MySQL (localhost:10053) + live PostgreSQL (Docker 127.0.0.1:5432, headless-sync-platform-postgres). All four P0-S4 DoD items proven: (1) happy-path relay, (2) idempotent re-relay via ON CONFLICT DO NOTHING, (3) crash-safety — PG row survives MySQL rollback, recovery tick produces no duplicate, (4) SKIP LOCKED concurrency — Worker B finds zero rows while Worker A holds locks. Full suite: 99/99 tests pass.

---

### FLAG-P0S4-3 — `created_at` UTC fidelity on relay: binding and assertion both weak

**Raised:** 2026-06-22 | **Session:** P0-S4 close | **Status:** Resolved — P0-S7 (2026-06-22)

**Resolution:** (1) `RelayWorkerStrategy::insertIntoSystemEvents()` already appended `'+00:00'` to both `source_updated_at` and `created_at` bindings (`$row['created_at'] . '+00:00'`), cast via `$12::timestamptz`. PostgreSQL interprets the `+00:00` suffix as UTC, guaranteeing the stored TIMESTAMPTZ reflects capture time in UTC regardless of session timezone. (2) `RelayEndToEndTest::test_pending_row_is_relayed_and_marked_relayed` strengthened: now asserts the full captured UTC datetime string (`assertStringContainsString($captureUtc, ...)`) AND that the PG value ends with an explicit UTC offset (`assertMatchesRegularExpression('/\+00(:00)?$/', ...)`). The `insertOutboxRow()` helper gains an optional `captureAt` parameter so the test can pin the timestamp before insertion.

---

### FLAG-P0S5-1 — Three structurally identical pg_* connection wrappers

**Raised:** 2026-06-22 | **Session:** P0-S5 | **Status:** Resolved — DECISION E (v1.5, 2026-06-22)

**Resolution:** DECISION E in `docs/ARCHITECTURE_DECISIONS.md` v1.5 rules that runtime DML subsystems
share a single PostgreSQL connection abstraction, with consolidation deferred to P0-S7. The three
existing wrappers are accepted temporary duplication. P0-S6 binding constraint: no new raw `pg_*`
wrapper may be introduced. P0-S7 authorised scope: collapse `OutboxConnectionInterface` and
`QueueConnectionInterface` into a shared `DatabaseConnectionInterface` under `core/Database/`.

---

### FLAG-P0S7-1 — DECISION E collapse interpretation: marker-interface vs full split

**Raised:** 2026-06-23 | **Session:** P0-S7 | **Status:** Resolved — DECISION E v1.6 (2026-06-23)

**Resolution (architect's ruling, DECISION E v1.6 — Split):**
- QUEUE: collapsed fully — `QueueConnectionInterface` deleted; `DatabaseQueueConnection` and `DatabaseQueueProvider` now depend on `DatabaseConnectionInterface` directly.
- OUTBOX: split by persistence technology — `PgsqlOutboxConnection` implements `DatabaseConnectionInterface` (PG delivery path); `MysqliOutboxConnection` implements new `MysqlOutboxConnectionInterface` (MySQL capture path). `OutboxConnectionInterface` deleted. The two contracts share no inheritance; `DatabaseConnectionInterface` is PostgreSQL-only.
- `RelayWorkerStrategy` holds one `MysqlOutboxConnectionInterface` + one `DatabaseConnectionInterface`, treating them as explicitly distinct abstractions.
- Rollback swallow semantics preserved: `PostgresDatabaseConnection::rollback()` matches historical behaviour from git commit 084456a — false return from `pg_query('ROLLBACK')` silently discarded, no exception thrown.
- All split fakes, service providers, integration tests, and the `FakeQueueConnection` updated to match.
- 204 unit + 18 integration tests pass; 0 skipped.

---

### FLAG-P1AS1-1 — Unpublish transition (publish→draft/pending/private/future) emits zero events

**Raised:** 2026-06-23 | **Session:** P1A-S1 review | **Status:** Resolved — OPEN-10 ruling applied, P1A-S1 close (2026-06-23)

**Resolution:** OPEN-10 (Resolved) — Option A approved. Membership-based capture: public set = `{publish}` only. `HookWiring::onTransitionPostStatus` updated with `$wasPublic`/`$isPublic` booleans; all four exit transitions (`publish → draft/pending/private/future`) now emit `content.{type}.deleted`. `wp_trash_post` suppressed by `$handledByTransition` guard when `transition_post_status` already handled the post_id (double-emit prevention). All nine new OPEN-10 tests pass. Full suite: 363 tests, 0 failures.

---

### FLAG-P1AS3-1 — `CanonicalModelInterface::getChecksum()` scope and DECISION 3 write-suppress compatibility

**Raised:** 2026-06-23 | **Session:** P1A-S3 | **Status:** Resolved — OPEN-11 (2026-06-23)

`CanonicalModelInterface::getChecksum()` doc-comment states: *"sha256 checksum of the canonical representation; used for write-suppress comparison against the stored projection checksum — DECISION 3."* DECISION 3 requires write-suppress to compare a **freshly-computed projection checksum** against the stored `content.*` checksum.

These two are compatible **only if** the canonical model and the PostgreSQL projection are a lossless reshape of the same fields — i.e. the adapter stores exactly what the canonical model contains and nothing else contributes to the stored checksum. If the adapter projection adds, drops, or transforms any field relative to the canonical model (e.g. computes a `uri` column from `slug`, or omits `meta` from the stored checksum), then `canonical.getChecksum()` will diverge from a write-side recomputed projection checksum, and write-suppress will either falsely skip writes or falsely execute them on every sync.

**P1A-S4 entry condition:** Before wiring write-suppress in the adapter, the architect must rule on one of:
- **Option A** — The adapter uses `canonical.getChecksum()` directly as the stored checksum. The projection schema must be a lossless reshape of every canonical field. No adapter-side field additions or omissions contribute to the stored checksum.
- **Option B** — The adapter computes a separate projection-shaped checksum over only the columns it writes to `content.*`. The stored checksum diverges from `canonical.getChecksum()`. `CanonicalModelInterface::getChecksum()` becomes unused or repurposed.

**Resolution:** Option A approved (OPEN-11, 2026-06-23). The Phase 1A delivery projection is a lossless reshape of the canonical model: no canonical field omitted, no derived columns added (precomputed URI variants, search vectors, denormalized aggregates, and analytics/ranking columns are explicitly excluded from Phase 1A). The adapter persists `canonical.getChecksum()` directly as the stored `content.*` checksum. Write-suppression compares the stored checksum against the canonical checksum — no second projection-shaped checksum path exists in Phase 1A. When a future projection intentionally diverges, it must compute and persist its own projection checksum, and that divergence requires a future ADR before implementation.

---

### FLAG-P1AS4-1 — content.entity_taxonomies column shape

**Raised:** 2026-06-23 | **Session:** P1A-S4 | **Status:** Resolved — ARCHITECTURE_DECISIONS.md v1.8 (2026-06-23)

**Resolution:** Pure join table. `(entity_id UUID, taxonomy_id UUID)` composite PK only. No timestamps, checksums, or metadata unless a future ADR explicitly requires relationship attributes. Migration `0005_create_content_entity_taxonomies.sql` frozen accordingly.

---

### FLAG-P1AS4-2 — aggregate_versions upsert monotonicity

**Raised:** 2026-06-23 | **Session:** P1A-S4 | **Status:** Resolved — ARCHITECTURE_DECISIONS.md v1.8 (2026-06-23); amended by DECISION J v1.10 (2026-06-24)

**Resolution:** Monotonic guard adopted. The `system.aggregate_versions` upsert in all three adapters uses `GREATEST()` so `latest_processed_version` only ever advances. Worker Resolve step remains the primary stale-event guard; the database GREATEST() guard provides defense-in-depth. Integration test `test_aggregate_versions_never_regresses_on_out_of_order_delivery` confirms the guard holds against live PostgreSQL.

**Amendment (DECISION J, 2026-06-24 — `docs/ARCHITECTURE_DECISIONS.md` v1.10):** The v1.8 ruling said "worker owns stale-event detection" without specifying the exact location in the pipeline. DECISION J clarifies: the **Resolve stage** (Step 4 of the Doc 8 §7 pipeline) is the PRIMARY, authoritative stale-event gate — `EventWorkerStrategy` performs a PG read of `system.aggregate_versions` before handler invocation and terminates early if the event is stale. The adapter-side FOR UPDATE + GREATEST guard is MANDATORY defense-in-depth for the TOCTOU window. Both layers are binding; neither replaces the other.

---

### FLAG-P1AS4-3 — `bulkPersist()` version guard and event recording

**Raised:** 2026-06-23 | **Session:** P1A-S4 close | **Status:** Resolved — architect ruling 2026-06-23

**Resolution (architect, 2026-06-23):** Option B approved. `persist()` is the ONLY supported persistence entry point in Phase 1A. `bulkPersist()` stays on `AdapterInterface` (signature unchanged) but performs NO projection writes in Phase 1A. All three adapters implement it as `throw new \LogicException('bulkPersist() is not implemented in Phase 1A.');` — no transaction, no execute, no partial path. The correct guarded batch path (events + version context, same guarantees as `persist()`) is deferred to a future ADR that lands with the first batch-with-events caller. Recorded in `docs/ARCHITECTURE_DECISIONS.md` under FLAG-P1AS4-3.

---

### FLAG-P1AS5-1 — IMPLEMENTATION_PLAN.md §4 five-bullet undercount

**Raised:** 2026-06-24 | **Session:** P1A-S5 | **Status:** Resolved — P1A-S7 (2026-06-25)

The Phase 1A deliverables list in IMPLEMENTATION_PLAN.md §4 showed only five REST endpoint bullets (missing `GET /hsp/v1/categories/{slug}`). The sixth endpoint was built and tested in P1A-S5 (Session Map authoritative per §1).

**Resolution (P1A-S7, 2026-06-25):** IMPLEMENTATION_PLAN.md §4 endpoint list reconciled to six bullets (`/hsp/v1/…`) during the namespace rename pass. All six endpoints now appear in the deliverables list. No code change was needed — the implementation was already correct.

---

### FLAG-P1AS6-2 — Plugin bootstrap incomplete: modules never boot; REST routes never register

**Raised:** 2026-06-24 | **Session:** P1A-S6 | **Status:** Resolved — P1A-S6a (2026-06-24)

**What is missing (three separate gaps):**

**Gap A — `Application::boot()` never calls `module.registrar::registerAll()`.**  
`Application::boot()` calls `$bootstrapper->bootstrap()` which builds the container, but `ModuleRegistrar::registerAll()` (which runs `discovery → register → boot`) is never called. Modules are never discovered; no hooks fire; no REST routes register.

**Gap B — `ModuleLoader::load()` calls `new $class()` with no constructor arguments.**  
`ContentModule` requires `(HookWiring $hookWiring, EventProviderInterface $eventProvider)` in its constructor. `ModuleLoader::load()` does `new $class()` — this would throw a fatal error if module loading were ever reached.

**Gap C — `ContentModule` has no REST wiring.**  
`ContentModule::register()` calls only `$this->hookWiring->register()`. There is no `add_action('rest_api_init', ...)` call anywhere in the Content module, so `ContentRestRegistrar::register()` is never invoked.

**Impact on P1A-S6 DoD:**

All E2E DoD items require the plugin to actually function:
- Without Gap A fixed: no WordPress hooks fire, no outbox events are captured.
- Without Gap B fixed: module loading crashes if discovery runs.
- Without Gap C fixed: REST API routes are never registered (`api/v1` namespace absent from WP REST index).

**Files involved (all out-of-scope per session brief):**
- `bootstrap/Application.php` — needs a call to `$container->get('module.registrar')->registerAll()` after `bootstrap()`
- `core/Module/ModuleLoader.php` — needs DI-aware construction (accept Container or a factory, not `new $class()`)
- `modules/Content/ContentModule.php` — needs REST registrar wired via `add_action('rest_api_init', ...)`

**Resolution (P1A-S6a, 2026-06-24 — Option B applied):**

Option B selected — new session P1A-S6a authorized for all three gaps:
- **Gap A:** `Application::boot()` now calls `$this->container->get('module.registrar')->registerAll()` after `bootstrap()`.
- **Gap B:** `ModuleLoader` refactored to accept `Container` via constructor injection; `load()` resolves via `$container->get($class)` when an explicit binding exists, falls back to `new $class()` only for zero-required-arg constructors, throws `InvalidManifestException` if required args exist but no binding. No reflection-based autowiring. `ContentModule` binding added in new `ContentServiceProvider`.
- **Gap C:** `ContentModule` receives a `ContentRestRegistrarFactory` (typed composition-root factory object, ADR-012 clean) via constructor injection; `boot()` wires `add_action('rest_api_init', ...)` with a closure that invokes the factory. `ContentModule` holds no `Container` reference (FLAG-P1AS6A-5 resolved). `ContentRestRegistrarFactory` is a new `final class` in `modules/Content/Rest/`; it receives per-dep factory closures from `ContentServiceProvider` and defers PG connection to first `__invoke()` call.
- `ContentServiceProvider` registered in `ContainerBuilder` (explicit, before `ModuleServiceProvider`).
- `ModuleServiceProvider` passes `Container` to `ModuleLoader`.
- Suite: 734/734 pass (12 new tests total: 3 `ModuleLoaderTest` + 6 `ContentModuleBootTest` original + 3 new ADR-012/lazy-deferral assertions). 0 skipped (with live DB env vars). 1 pre-existing deprecation unchanged.

---

### FLAG-P1AS6A-1 — QueueServiceProvider: new DatabaseConnectionInterface singleton touches P0-S5 / DECISION E v1.6

**Raised:** 2026-06-24 | **Session:** P1A-S6a (out-of-scope change) | **Status:** Resolved — P1A-S6c (2026-06-24)

**Resolution (P1A-S6c, 2026-06-24 — DECISION K applied):** `DatabaseConnectionInterface` singleton binding removed from `QueueServiceProvider`. New `DeliveryServiceProvider` (`core/Container/Definitions/DeliveryServiceProvider.php`) opens a dedicated connection with `PGSQL_CONNECT_FORCE_NEW`, wrapping `PostgresDatabaseConnection` (no new pg_* wrapper). `ContainerBuilder` registers `DeliveryServiceProvider` before `WorkerServiceProvider` and `ContentServiceProvider`. Integration test `DeliveryConnectionIsolationTest` (5 tests) proves all 5 DoD-4 items against live PostgreSQL. DECISION K recorded in `docs/ARCHITECTURE_DECISIONS.md` v1.11. IMPLEMENTATION_PLAN.md §5b: P1A-S6c inserted; P1A-S6 Depends-on = P1A-S6c. Full suite: 789/789, 0 failures, 0 skipped (live DB).

**What changed:** `QueueServiceProvider` received a new `singleton(DatabaseConnectionInterface::class, ...)` binding (lines 38–55) that opens a `\pg_connect()` connection without `PGSQL_CONNECT_FORCE_NEW` and returns a `PostgresDatabaseConnection` wrapper. This was added to satisfy `ContentServiceProvider`'s query-provider dependencies, which need `DatabaseConnectionInterface`.

**Scope extended (P1A-S6b, 2026-06-24):** This binding is now also exercised at runtime by `EventWorkerStrategy`'s Resolve-stage stale-guard read. `WorkerServiceProvider` injects `$c->get(DatabaseConnectionInterface::class)` as the third constructor arg to `EventWorkerStrategy`; the Resolve-stage `isStale()` call performs a non-locking `SELECT` on `system.aggregate_versions` through this shared connection. The blast radius of this flag therefore covers not only REST delivery queries (`ContentRestRegistrar` / query providers) but also the worker Resolve-stage PG read path — both are live in P1A-S6b.

**Frozen sessions / docs touched:**
- **P0-S5:** Introduced `DatabaseQueueConnection` wrapping a `PGSQL_CONNECT_FORCE_NEW` handle specifically to prevent connection-pool sharing on the `SKIP LOCKED` claim path. The new binding opens a *second* connection via the same DSN without `FORCE_NEW`. PHP's libpq may return a pooled handle for the same DSN if one already exists in the process (e.g., `'outbox.connection.pgsql'` in `OutboxServiceProvider` uses the same DSN). Connection sharing between `DatabaseConnectionInterface` (REST delivery OR Resolve-stage worker read) and any concurrent transactional consumer in the same process would be unsafe.
- **DECISION E v1.6:** Authorized `DatabaseConnectionInterface` as the shared runtime PG abstraction but scoped consolidation to P0-S7 and declared: *"no new raw `pg_*` wrapper may be introduced in P0-S6."* This binding introduces a new `pg_connect()` call outside the three previously accepted wrappers.

**Ruling needed:** (a) Is opening a second `pg_connect()` without `FORCE_NEW` for REST delivery queries and the worker Resolve-stage read acceptable, given that libpq may pool it with `outbox.connection.pgsql`? (b) Should `DatabaseConnectionInterface` be sourced from an already-authorized connection instead (e.g., share `outbox.connection.pgsql` or a dedicated delivery connection with `FORCE_NEW`)? (c) Does this placement in `QueueServiceProvider` (rather than a dedicated `DeliveryServiceProvider`) violate DECISION E's allocation intent?

---

### FLAG-P1AS6A-2 — pg_connect prefix fixes in QueueServiceProvider and OutboxServiceProvider

**Raised:** 2026-06-24 | **Session:** P1A-S6a (out-of-scope change) | **Status:** Recorded and kept — no runtime regression; provider factory closures remain integration-test-uncovered

**What changed:** All occurrences of `pg_connect(` in `QueueServiceProvider` and `OutboxServiceProvider` were prefixed with `\` to produce `\pg_connect(`. The change is correctness cleanup: in PHP, an unqualified function call inside a namespaced class resolves to the current namespace first, then falls back to global. Since `HSP\Core\Container\Definitions\pg_connect` does not exist, PHP's fallback to `\pg_connect()` applied at runtime in all prior sessions — the calls were never broken. The prefix eliminates the fallback lookup.

**Frozen sessions / docs touched:**
- **P0-S4 / P0-S5 "live DB proofs":** The integration tests for those sessions (`RelayEndToEndTest`, `DatabaseQueueProviderIntegrationTest`) bypassed the service-provider factory closures entirely — they injected raw `pg_connect()` handles directly in the test setup and never invoked `OutboxServiceProvider` or `QueueServiceProvider` factory code. The prefix fix does not invalidate those test results, but it means the factory closures themselves were never exercised by the integration test suite.

**Ruling needed:** Confirm the fix is accepted as a correctness cleanup. No runtime regression possible (PHP's global-namespace fallback was always in effect). Note for the record that P0-S4/P0-S5 provider factory closures remain integration-test-uncovered.

---

### FLAG-P1AS6A-3 — Bootstrapper.php: modulesBasePath parameter surfaces a P0-S3 discovery gap

**Raised:** 2026-06-24 | **Session:** P1A-S6a (out-of-scope change) | **Status:** Recorded and kept — fix accepted; P0-S3 fixture-only discovery noted; real-filesystem integration test deferred

**What changed:** `Bootstrapper::bootstrap()` gained a `string $modulesBasePath = ''` parameter (passed from `Application::boot()` which derives it from `HSP_PLUGIN_DIR . 'modules/'`). `ContainerBuilder::build()` already accepted `$modulesBasePath` as its second parameter; the gap was that `Bootstrapper` was not forwarding it.

**Frozen sessions / docs touched:**
- **P0-S3 DoD:** P0-S3 claimed "module registry discovers modules via module.json." That was proven only via unit tests that inject a fake `ModuleLoader` and never invoke `ModuleDiscovery::discover()` against the real filesystem. Without the `modulesBasePath` fix, `glob('/*/module.json')` on the real boot path returns `[]` silently — no modules were ever discovered in production. The P0-S3 live-boot DoD item was not actually proven.

**Ruling needed:** (a) Confirm the fix is accepted. (b) Rule on whether P0-S3's DoD is considered retroactively satisfied (modules now discover correctly at live boot), or whether a P0-S3 integration test covering real-filesystem discovery should be added before P1A-S6a is approved.

---

### FLAG-P1AS6A-4 — headless-sync.php: pgsql extension guard

**Raised:** 2026-06-24 | **Session:** P1A-S6a (out-of-scope change) | **Status:** Recorded and kept — accepted as justified defensive measure at entry point

**What changed:** `headless-sync.php` received an `extension_loaded('pgsql')` check immediately after the `ABSPATH` guard. If `pgsql` is not loaded, an `admin_notices` action is registered to show an error banner, and the file returns early — the autoloader and `Application` are never initialized.

**Frozen sessions / docs touched:**
- No frozen ADR is directly contradicted. The change is a plugin-activation guard. The scope concern is that `headless-sync.php` is the plugin entry point; changes to it were not in the P1A-S6a brief. The CLAUDE.md session-close rule requires out-of-scope file changes to be "reverted or flagged."

**Ruling needed:** Confirm whether the pgsql guard is accepted as a justified defensive measure at the entry point, or whether it should be reverted and re-introduced in a dedicated session with a test covering the early-return path.

---

### FLAG-P1AS6A-5 — Gap C lazy closure: deferred $c->get() in business logic (ADR-012 boundary)

**Raised:** 2026-06-24 | **Session:** P1A-S6a | **Status:** Resolved — P1A-S6a review pass (2026-06-24)

**Resolution:** Replaced the bare `\Closure` capturing `$c` with a typed `ContentRestRegistrarFactory` final class (`modules/Content/Rest/ContentRestRegistrarFactory.php`). The factory receives six per-dep factory closures via constructor injection (defined in `ContentServiceProvider`); it holds no `Container` reference. `ContentModule::$restRegistrarFactory` is typed `ContentRestRegistrarFactory` — grep of `ContentModule.php` finds no `Container::get`, `$container->get`, or `global $container`. Lazy deferral preserved: `DatabaseConnectionInterface` (PG connection) is not resolved until `ContentRestRegistrarFactory::__invoke()` is called at `rest_api_init` time. Proven by `testContentRestRegistrarIsNotConstructedAtModuleLoadTime` (spy counter = 0 at module-load, = 1 at first factory invocation).

---

### FLAG-OPSS1-1 — Heartbeat/metrics persistence: no worker-health table was ever frozen

**Raised:** 2026-07-11 | **Session:** OPS-S1 (pre-implementation) | **Status:** Resolved — DECISION P + DECISION Q (ARCHITECTURE_DECISIONS.md v1.16, 2026-07-11)

**Resolution (architect rulings 1 & 2, 2026-07-11):** DECISION P freezes a single current-state table `system.worker_heartbeats` (worker_id UUID PK, worker_type TEXT, status TEXT, last_heartbeat_at TIMESTAMPTZ, started_at TIMESTAMPTZ), upserted per tick, no history; `DatabaseHeartbeatPublisher` implements the existing `HeartbeatPublisherInterface`, connection injected via constructor (ADR-012) using the worker-runtime handle (DECISION L Ruling 0 — no new handle/class/pg_* wrapper). A migration creating the table is authorized for OPS-S1. DECISION Q rules out any metrics table: derived metrics computed on demand from PostgreSQL; runtime counters emitted as structured worker log events; "metrics emit" DoD = queryable status + structured logs. The connection-binding question in (d) is answered by DECISION L Ruling 0: heartbeat is worker-runtime infrastructure on the existing worker-runtime connection, not the delivery handle. Both heartbeat "visible/updated per tick" and metrics "emit" DoD items are now buildable.

OPS-S1 DoD requires heartbeat to be "visible (worker_id UUID, status, last_heartbeat_at TIMESTAMPTZ) and updated per tick" and requires the Doc 8 §27 minimum metric set to "emit". Both imply durable, queryable, cross-process storage — a monitor detecting a crashed worker by heartbeat age (Doc 8 §15) must read a store, and the current `NullHeartbeatPublisher` (`core/Workers/NullHeartbeatPublisher.php`) discards records.

**The conflict:** No `system.worker_heartbeats` / `system.worker_metrics` table (or any heartbeat/metrics DDL) exists in the frozen schema, and none is present in the ARCHITECTURE_DECISIONS.md Implications table. OPEN-3 froze only the expansion of `system.dead_letter_jobs`. Doc 8 §15 (heartbeat field list) and §27 (metric list) describe *intent* only — no column-level DDL was ever frozen the way OPEN-8 froze schema/module/security tables.

The OPS-S1 session brief permits a new migration "ONLY if OPEN-3 v1.1 DLQ schema requires a column not yet present." Migration `0004_create_system_dead_letter_jobs.sql` already carries all four OPEN-3 columns (`stack_trace`, `attempt_count`, `worker_id`, `payload_snapshot`), so that clause is **not** triggered. A heartbeat/metrics table is a *separate* table, outside that narrow authorization. Writing it without a ruling would violate the CLAUDE.md freeze rule (§5: never silently resolve a conflict with a frozen doc; a migration diverging from a frozen ruling may not be left in the tree).

**Ruling needed:** (a) Freeze a heartbeat/metrics table contract (an OPEN-N / ADR analogous to OPEN-3), including whether it is one table or two (`worker_heartbeats` vs `worker_metrics`), the exact column set and types (Doc 8 §15 lists `worker_id, status, started_at, last_heartbeat_at, current_job, memory_usage, processed_count`; §27 lists `jobs_processed, jobs_failed, jobs_retried, jobs_dead_lettered, average_processing_time, memory_usage, worker_uptime`), and PK/upsert semantics (one row per `worker_id`, upserted per tick, vs append-only history). (b) Confirm the type canon applies (`worker_id UUID`, all timestamps `TIMESTAMPTZ`). (c) Authorize a new migration `0012_*` under this new ruling, OR direct a non-DDL alternative (in-process/log-only publisher) and accept the weaker "visible" semantics. (d) Confirm which container binding the real `HeartbeatPublisher` writes through — the connection topology is FROZEN pending FLAG-P1AS6D-1, so the publisher must reuse an existing FORCE_NEW handle via constructor injection (ADR-012); a ruling is needed on *which* one (delivery `DatabaseConnectionInterface` per DECISION K is a candidate, but heartbeat is system-side DML, not delivery — this may collide with the same relay/queue-vs-delivery split that produced FLAG-P1AS6D-1).

**Resolution trigger:** Architect freezes the heartbeat/metrics table DDL (or rules out a table) and names the connection binding. Until then, the heartbeat "visible/updated per tick" and metrics "emit" DoD items cannot be built without improvising a frozen-schema change.

---

### FLAG-OPSS1-2 — Metric counters have no source of truth in the current pipeline

**Raised:** 2026-07-11 | **Session:** OPS-S1 (pre-implementation) | **Status:** Resolved — DECISION Q (ARCHITECTURE_DECISIONS.md v1.16, 2026-07-11)

**Resolution (architect ruling 2, 2026-07-11):** DECISION Q defines the producer as (1) on-demand PostgreSQL aggregates for derived metrics (queue depth, DLQ depth, oldest-pending age, worker count) and (2) structured worker log events for runtime counters (processed/retry/failure/replay). No metrics table, no rollups, no `average_processing_time` timing column on the frozen `system.queue_jobs` schema, no external telemetry backend in MVP. The "no source of truth" gap is closed by defining the source as on-demand queries + log emission rather than a persisted counter sink.

The Doc 8 §27 minimum metric set (`jobs_processed`, `jobs_failed`, `jobs_retried`, `jobs_dead_lettered`, `average_processing_time`, `memory_usage`, `worker_uptime`) has no producer today. `EventWorkerStrategy::execute()` calls `complete()` / `release()` / `deadLetter()` on the queue but increments no counters, records no per-job timing, and the `WorkerEngine` tick loop measures no duration. There is no metrics sink.

**Ruling/direction needed:** (a) Where are counters accumulated — in-process on the `WorkerEngine` (reset per process, exposed via heartbeat row) or derived by aggregate query over `system.queue_jobs` / `system.dead_letter_jobs` (e.g. `jobs_dead_lettered = COUNT(*) FROM system.dead_letter_jobs`)? (b) Is `average_processing_time` a rolling in-process average or a stored per-job timing column (which would need the queue_jobs schema — frozen — to gain a column, itself a freeze conflict)? (c) Does the metric set persist to the same table decided in FLAG-OPSS1-1, or emit to logs/statsd only? These are coupled to FLAG-OPSS1-1's table decision and cannot be built until it is ruled.

---

### FLAG-OPSS1-3 — Live crash→visibility-timeout requeue has no runtime driver

**Raised:** 2026-07-11 | **Session:** OPS-S1 (pre-implementation) | **Status:** Resolved — DECISION R (ARCHITECTURE_DECISIONS.md v1.16, 2026-07-11)

**Resolution (architect ruling 3, 2026-07-11):** DECISION R names `MaintenanceWorkerStrategy` the runtime driver for `requeueTimedOut()`, un-stubbed in OPS-S1 (scope `core/Workers/`). Cadence is configuration-driven with a sensible default — no hardcoded timing values — following the OPEN-4 config-driven-timeout precedent. The recovery loop uses the worker-runtime handle (DECISION L Ruling 0), not a new binding. The DoD is satisfied by the maintenance-tick driver plus the integration test exercising `requeueTimedOut()`; no full production scheduler (WP-Cron/systemd wiring) is pulled into OPS-S1.

`DatabaseQueueProvider::requeueTimedOut($queueName)` is implemented and unit/integration-tested, but nothing invokes it on a runtime loop. `MaintenanceWorkerStrategy` (the natural home, per its own STUB comment) is a stub returning `false`; no scheduler, no WP-Cron fallback, and no worker tick calls `requeueTimedOut()`. The DoD requires a *simulated worker crash triggers visibility-timeout requeue, proven by integration test* — the proof is straightforward (claim, let visibility expire, call `requeueTimedOut`, assert re-`available`), but a **runtime** driver must own the recovery call in production.

**Ruling/direction needed:** (a) Does OPS-S1 implement `MaintenanceWorkerStrategy::execute()` to call `requeueTimedOut()` for each partition on a cadence (in scope: `core/Workers/`), and if so what cadence/config key? (b) Is the DoD satisfied by an integration test exercising `requeueTimedOut()` directly plus a thin maintenance-tick driver, or is a full production scheduler expected (which would pull in WP-Cron/systemd wiring that reads as out-of-scope for OPS-S1)? (c) Which connection binding does the maintenance/requeue loop use (same FROZEN-topology question as FLAG-OPSS1-1(d))?

---

### FLAG-OPSS1-4 — Single-event replay entry point and DLQ-inspect tooling surface unspecified

**Raised:** 2026-07-11 | **Session:** OPS-S1 (pre-implementation) | **Status:** Resolved — DECISION S (ARCHITECTURE_DECISIONS.md v1.16, 2026-07-11)

**Resolution (architect ruling 4, 2026-07-11):** DECISION S rules: (a) surface is **WP-CLI only** (`hsp dlq list | inspect | replay`), no admin UI — sidestepping the TBD WP-boundary coding-standard question. (b) DLQ rows are permanent audit records, never deleted; replay stamps a new `replayed_at TIMESTAMPTZ NULL` column. That column is **absent** from the OPEN-3 v1.1 schema (verified against migration 0004, which carries only the four OPEN-3 deltas) — adding it via a forward migration is authorized in OPS-S1 scope (0004 not edited). (c) Replay runs in ONE PG transaction: verify DLQ row exists → verify `replayed_at IS NULL` → **DELETE any `system.queue_jobs` row sharing the `event_id`** → INSERT fresh job `attempts = 0` → stamp `replayed_at`. The DELETE step is what defeats the `UNIQUE(event_id)` silent-no-op trap this flag identified: DECISION L (d) retains completed/dead-lettered rows, so a naive re-`enqueueIdempotent()` would `DO NOTHING`; clearing the prior row first makes the fresh insert take effect. Replay re-enters via the normal queue/claim path and passes the DECISION J Resolve-stage stale guard — an already-current aggregate acks with zero projection writes, which is correct behavior.

`ReplayWorkerStrategy` is a P0-S6 stub returning `false`. The brief requires replay to "re-enter the pipeline through the normal queue path (enqueueIdempotent / existing claim semantics) — never write projections directly." `enqueueIdempotent(eventId, queueName)` exists and is the correct re-entry primitive. However the operational *surface* is unspecified:

**Ruling/direction needed:** (a) What triggers a single-event replay — a WP-CLI command, a WP admin action, or a plain PHP tool under `headless-sync/tools/` (like `smoke_e2e.php`)? The brief lists "admin/CLI DLQ tooling" in scope but does not pick one; coding-standard/WPCS enforcement at the WP boundary is still TBD (CLAUDE.md), which affects an admin-page implementation. (b) On replay, is the DLQ row deleted, marked replayed, or left intact (audit)? `system.dead_letter_jobs` has no `status`/`replayed_at` column — marking it would be another frozen-schema change (ties to FLAG-OPSS1-1). (c) Does single-event replay re-`enqueueIdempotent()` the existing `event_id` (relying on the DECISION J Resolve-stage stale guard to no-op an already-current aggregate, and UNIQUE(event_id) to dedupe against a still-present queue row) — confirming the brief's "stale replay acking with zero writes is correct" — and is that the *entire* required behavior, or must replay also clear the completed/dead-lettered queue_jobs row so a fresh job can be claimed? (The existing `system.queue_jobs` UNIQUE(event_id) from migration 0011 means a replay of an event whose prior job row still exists will `DO NOTHING` — replay would then be a silent no-op unless the prior row is first cleared. This interaction must be ruled before building replay.)

---

### FLAG-P1AS6D-1 — Dispatcher connection topology: no relay/queue handle exposed separately from DECISION K delivery singleton

**Raised:** 2026-06-25 | **Session:** P1A-S6d | **Status:** Resolved — DECISION L Ruling 0 (ARCHITECTURE_DECISIONS.md v1.16, 2026-07-11)

**Resolution (architect ruling 0, 2026-07-11):** The four-connection topology is **ratified and FROZEN as final** — relay (`outbox.connection.pgsql`), queue/worker runtime (`queue.connection.pgsql`), delivery (`DatabaseConnectionInterface`, DECISION K), dispatcher (`dispatcher.connection.pgsql`, DECISION L). Answer to (a): **yes** — the fourth FORCE_NEW dispatcher handle is accepted as a pragmatic, ratified extension of the DECISION E (v1.6) temporary-duplication allowance. Answer to (b)/(c): consolidation remains a future-ADR concern, not required now; DECISION L is amended to record the four-handle topology explicitly. **No fifth handle may ever be introduced without a new ADR.** Heartbeat publication (DECISION P) reuses the existing worker-runtime handle and adds no new handle/class/pg_* wrapper.

After P1A-S6c (`DeliveryServiceProvider`) moved the `DatabaseConnectionInterface` singleton to the dedicated delivery FORCE_NEW handle, there is no container binding that exposes the relay/queue runtime PG handle separately from that delivery singleton.

The Dispatcher (relay/queue-side system DML) must NOT use the delivery singleton (DECISION K). Because no relay/queue-side handle was exposed as a resolvable binding, `DispatcherServiceProvider` opens its own third FORCE_NEW handle bound as `'dispatcher.connection.pgsql'`. This resolves the immediate connection isolation requirement and is what was built in P1A-S6d.

However, this is a connection-topology decision beyond the original P1A-S6d ruling:

- The accepted connection set after S6c was: relay handle (`outbox.connection.pgsql`), queue-claim handle (`queue.connection.pgsql`), delivery handle (`DatabaseConnectionInterface`).
- P1A-S6d adds a fourth handle: `'dispatcher.connection.pgsql'`.
- DECISION E v1.6 accepted three handles as "temporary duplication" and authorized consolidation in P0-S7. A fourth handle extends that accepted duplication without a new ruling.

**Ruling needed:** (a) Is opening a fourth FORCE_NEW handle for the Dispatcher accepted as a pragmatic extension of the DECISION E temporary-duplication allowance? (b) Should `'dispatcher.connection.pgsql'` eventually share the relay/queue handle once that handle is exposed as a container binding? (c) Does this topology require an amendment to DECISION E or DECISION K, or is it subsumed by DECISION L?

**Resolution trigger:** Architect ruling on the fourth-handle question. If accepted, update DECISION L to explicitly record the four-handle topology. If consolidation is preferred, expose `'outbox.connection.pgsql'` as a container binding and rewire `DispatcherServiceProvider` to resolve it.

---

### FLAG-P1AS6-1 — Missing Content event handler layer: queue → PG projection never implemented

**Raised:** 2026-06-24 | **Session:** P1A-S6 | **Status:** Partially resolved — architect rulings issued 2026-06-24; implementation deferred to P1A-S6b

**What is missing:**

`EventWorkerStrategy::executeHandler()` (introduced in P0-S6) contains an explicit stub with comment:
> *"P1A-S1 TODO: resolve subscriber from EventRegistry; invoke handler; handler commits its own PG transaction (DECISION 3)."*

No Subscribers, Handlers, or event-handler wiring exists anywhere in `modules/Content/`. The P1A-S1 session log records HookWiring and EventProvider only — the handler layer was not built. No session P1A-S2 through P1A-S5 added it either. The `EventRegistry` API (`register(eventType, callable)`) is fully implemented in core but nothing registers content event handlers. `ContentModule::register()` calls only `$this->hookWiring->register()`.

**Impact on P1A-S6 DoD:**

The following E2E DoD items **cannot be satisfied** without the handler layer:
- End-to-end sync (WP edit → outbox → relay → queue → worker → PG projection)
- Sync delay < 30s SLA
- API endpoints return correct data (no projection rows exist without the handler)
- Three-op atomicity under live conditions
- Idempotency under live conditions
- Stale-event skip under live conditions
- Next.js pages reflect live WP content (depends on projection data)

The live infrastructure (MySQL, PostgreSQL Docker, WordPress, worker engine) is fully available. The gap is in the business logic layer within `modules/Content/` scope.

**Architect rulings issued 2026-06-24 (pre-P1A-S6b):**

- **Worker State Loading (DECISION H — `docs/ARCHITECTURE_DECISIONS.md` v1.10):** Option B approved. Workers reload current WordPress state via a defined WP bootstrap path in the worker runtime. Event payload enrichment (Option A) rejected. Direct-MySQL reload (Option C) rejected. ADR-044 reaffirmed. Operational bootstrap details deferred to Doc 10 / ops session.

- **Delete Processing (DECISION I — `docs/ARCHITECTURE_DECISIONS.md` v1.10):** Option C approved. `content.*.deleted` events follow a dedicated tombstone path consuming only the event envelope (aggregate identity + metadata); performs soft-delete projection; no reload, no extract, no transform. `AdapterInterface` gains `tombstone()` method. Canonical models and OPEN-11 checksum surface UNCHANGED. DECISION 3 three-op atomicity applies to the tombstone path.

- **Stale-Event Guard (DECISION J — `docs/ARCHITECTURE_DECISIONS.md` v1.10):** Resolve-stage guard is PRIMARY, authoritative stale-event gate. Adapter in-txn FOR UPDATE + GREATEST guard is MANDATORY defense-in-depth. Authorized for P1A-S6b: PG read dependency on `EventWorkerStrategy`, `WorkerServiceProvider` wiring, Resolve-stage aggregate-version lookup, early termination before handler execution.

**Resolution trigger:** FLAG is fully resolved when P1A-S6b ships the Content Subscriber/Handler spine implementing all three rulings above.

---

### FLAG-GATES1-1 — Entity and date-range replay modes not implemented; GATE-S1 criterion 2 fails

**Raised:** 2026-07-12 | **Session:** GATE-S1 | **Status:** **RESOLVED (2026-07-12, GATE-S1 resume — DECISION T + gate re-run).** The feature gap this flag records is closed (OPS-S2 shipped single-event [DECISION S], entity, and date-range [DECISION T] replay), and the GATE-S1 criterion-2 re-run has replaced the STOP-and-flag `markTestIncomplete` in `ReliabilityValidationTest` with real gate evidence: three passing live-DB tests proving each mode succeeds and converges the projection to correct final state (single-event shape + criterion-3 end-to-end; entity replay of a corrupt+drifted projection; date-range replay converging in-window aggregates while leaving the out-of-window aggregate untouched). Gate suite 6/6 green, 0 Incomplete; full suite 888 green. GATE-S1 criterion 2 is now PASS; the Reliability gate is done.

**Update (2026-07-12, OPS-S2):** Direction (a) below was actioned — a dedicated non-gate session (OPS-S2) was opened to build entity + date-range replay, and the Session Map row + GATE-S1 Depends-on were amended (now `OPS-S1, OPS-S2`). Before writing code, OPS-S2 hit a CRITICAL STOP CONDITION: direction (a)'s stated plan ("re-enqueue lifecycle analogous to DECISION S for each selected event, **passing the DECISION J stale guard**") does not actually hold for entity/date-range replay. Those modes target **already-successfully-processed** aggregates, whose `latest_processed_version` already equals the event's `aggregate_version`; the DECISION J guard therefore suppresses the re-enqueued event (`<=` → ack, zero writes) and **no reprojection occurs**. DLQ replay escapes this only because dead-lettered events were never processed (version still ahead of the guard). The reprojection mechanism is unspecified in Doc 4 §24 and the three governing constraints are mutually incompatible — see **FLAG-OPSS2-1** for the full analysis and the three options the architect must choose between. No replay code was written.

**What the gate requires (IMPLEMENTATION_PLAN.md §4 → Reliability Validation, criterion 2):**
> Replay succeeds for single event, entity, and date-range replay modes

**What exists:** Only **single-event** replay. Verified against current code:
- `core/Workers/Strategies/ReplayWorkerStrategy.php` is a **stub** (`execute()` returns `false`;
  comment: *"STUB — OPS-S1: implement single-event and entity replay modes."*).
- `core/Queue/DeadLetterRepository::replay(string $dlqId)` and the WP-CLI `hsp dlq replay <id>`
  (`core/Cli/DlqCommand::replay`) operate on a **single DLQ row id → single event_id** only
  (DECISION S lifecycle). There is no `replayEntity()` / `replayDateRange()` method anywhere in
  `core/`, `modules/`, or `tools/`.
- Doc 4 §24 lists four replay modes (Single Event, Entity, Date Range, Full) as *architecture*,
  but OPS-S1 shipped single-event only. The OPS-S1 Session Map row and Doc 11 §8 scope
  single-event + entity replay; **entity replay was not actually built**, and date-range is
  out of the Early Operational Baseline scope entirely.

**Note on the gate brief's parenthetical:** the GATE-S1 brief stated *"OPS-S1 shipped
single-event + entity only per Doc 4 §24 — verify."* Verification shows this is inaccurate:
**OPS-S1 shipped single-event only.** Entity replay is also absent.

**Why this is a STOP-and-flag, not a fix:** The gate session's scope is *test evidence only* —
"no production code changes unless a validation check fails (a failure is a STOP-and-flag, not a
fix-in-session)." Building entity + date-range replay is a feature-bearing change to `core/`
(`ReplayWorkerStrategy`, new repository methods, likely a replay-scope query over `system.events`
by aggregate / date range, and CLI surface). Per CLAUDE.md, replay features are not built in a
gate session, and the missing modes are not derivable from a frozen ruling.

**Ruling/direction needed:**
- (a) Authorize a dedicated (non-gate) session to implement **entity replay** and **date-range
  replay** (Doc 4 §24), including: the replay-scope selection query (all events for an aggregate;
  all events in a `[from, to]` window), the single-PG-transaction re-enqueue lifecycle
  (analogous to DECISION S for each selected event, passing the DECISION J stale guard), and the
  WP-CLI surface (`hsp replay entity <type> <id>` / `hsp replay range <from> <to>` or equivalent).
- (b) Confirm whether **Full Replay** (Doc 4 §24) is in or out of MVP gate scope (Phase 1A lists
  advanced replay under Phase 3 — the gate may accept single/entity/date-range and defer Full).
- (c) Until (a) ships and GATE-S1 criterion 2 turns PASS, the Architecture Validation Gate cannot
  pass; Phase 2 remains blocked.

**Resolution trigger:** An authorized session implements entity + date-range replay with named,
passing integration tests landing the correct final projection state for each mode; GATE-S1
criterion 2 is then re-run and flips to PASS. — **MET:** OPS-S2 implemented the modes under
DECISION T; the GATE-S1 resume (2026-07-12) re-ran criterion 2, which now PASSES with three
named gate tests. Flag resolved.

---

## Session Log

<!-- Append one line per session: YYYY-MM-DD | session ID | what shipped | flags raised -->

2026-07-16 | OPSC-S4 | Operational Actions — Replay + Reconcile ONLY (thin delegators) SHIPPED (DECISION V (d)/(e)/(f); DECISION T/S/U; ADR-053, ADR-051 HELD not cited). New core `core/Operations/Services/OperationsActionService` — the action-side mirror of OperationsService; a THIN DELEGATOR holding ONLY `worker.strategy.replay` (ReplayWorkerStrategy → ReplayService, DECISION T entity/range) + `worker.strategy.reconciliation` (ReconciliationWorkerStrategy → ReconciliationService, DECISION U drift/incremental/full) + StructuredLogger. Reaches NO DatabaseConnectionInterface/adapter/reader → **write-spy proof holds by construction**; repair is re-emission only, never a second repair path. `execute($key,$params)` routes replay entity/range + reconcile drift/incremental/full(+dry-run); permits ONLY `replay`/`reconcile` — rejects flush_queue/restart_workers/any other key. New DTO `core/Contracts/Operations/ActionResult` (action/ok/summary/count/detail; JSON-friendly, no infra). New wp-admin boundary `core/Operations/Admin/ConsoleActionController` — ONE nonce-protected admin-ajax action (`hsp_ops_action`): check_ajax_referer → resolve ConsoleAction via OperationsService (capability + confirmation policy discovered from descriptor, not hardcoded) → current_user_can → **confirmation** (descriptor confirmationRequired) → sanitize every input → delegate THROUGH OperationsActionService (DECISION V (b)). Talks only to OperationsService + OperationsActionService (ADR-053; added to the NoInfrastructureReachableFromUi reflection guard). Audit = one `operations.action` StructuredLogger line per executed action (existing observability path, DECISION Q) — zero new persistence/table/schema. Wiring: OperationsServiceProvider binds service+controller (strategies resolved lazily; WorkerServiceProvider registers first → order safe), ConsoleAdminRegistrar gains the action hook, and boot() registers EXACTLY two ConsoleActions (Replay+Reconcile, capability-gated + confirmation-required) — no Flush Queue (V (e)), no Restart Workers (V (f)). Worker status/heartbeat/runbook remain the read-only S2/S3 surface (untouched). Carryover fix (OPSC-S3 nit): PlaygroundRequestExecutor now selects by stable route key (METHOD /namespace/route, keyFor()) not positional index — PlaygroundView emits the key as the <option> value, ConsoleAjaxController passes the sanitized key, console.js posts it; order-shift between render/execute now resolves to the SAME endpoint (new stability test). Tests: 24 new Unit (OperationsActionServiceTest — all replay/reconcile modes + dry-run + audit-line + write-spy zero-direct-writes over REAL services + unknown/flush/restart rejection + param validation; ConsoleActionControllerTest — nonce/capability/confirmation/sanitize/route + invalid-params→400; ConsoleActionWiringTest — seam resolves, exactly [reconcile,replay] registered, no flush/restart under any key mechanically; Playground route-key + order-shift-stability updates) + 3 new live-MySQL(10053)+PG(5432) Integration (tests/Integration/Operations/OperationsActionIntegrationTest — replay-entity action converges a corrupt projection through the seam with WriteSpyConnection.executeCount==0; reconcile-drift action repairs a missed capture through the seam, zero direct writes; executed action emits one structured audit line). Reused reconciliation/replay harness (FakeWpStore/FakeWpdb/ReplayReadingLoader/StoreReconciliationSource/WriteSpyConnection). Full Unit 964/964 green, 0 failures/deprecations (was 940 + 24), deterministic on two consecutive runs; OPSC-S4 + related Integration 20/20 green on two runs; full Integration 136/137 — sole failure is GATE-S4 criterion-1's git-porcelain guard (in-scope core/ files uncommitted; identical to FLAG-OPSCS2-1, clears on commit). Verified: OPSC-S1/S2/S3 contracts unchanged except additive (ActionResult added; PlaygroundRequestExecutor signature updated for the nit); no migration, no new table, no new PG handle (four-handle topology unchanged — DECISION L Ruling 0), no new pg_* wrapper (DECISION E); git status shows only in-scope paths. | no new flags. OPSC-S4 DoD met; **Phase 1A – Expanded (Operations Console) COMPLETE — OPSC-S0..S4 all shipped**.

2026-07-15 | OPSC-S3 | Operations Console UI (server-rendered PHP) + API Playground SHIPPED (DECISION V (a)/(b); ADR-047/050/052/053; DECISION N/F). Pure renderers `core/Operations/UI/`: `Html` (escape-at-output helper — WP `esc_*` with `htmlspecialchars` fallback so the guarantee holds in unit tests); `DashboardView` (one read-only widget per snapshot shape — QueueStatus/HealthReport[]/MetricSample[]/WorkerStatus[] — + System Information + Module Inspector, every dynamic value escaped); `PlaygroundView` (Endpoint Explorer + Request Builder + Response Viewer over `EndpointDescriptor` metadata, no hardcoded routes). wp-admin boundary `core/Operations/Admin/`: `AdminPageController` (two MVP menu pages — Operations + API Playground — with per-page capabilities from `ConsolePage` descriptors; server-side render; `admin_enqueue_scripts` enqueue + nonce localize; `current_user_can`/`wp_die` guard on every render); `ConsoleAjaxController` (two nonce+capability-protected admin-ajax endpoints — poll returns serialized snapshots, execute runs one live GET — `check_ajax_referer` + capability + input sanitization at the boundary); `PlaygroundRequestExecutor` (in-process `rest_do_request` GET against published `hsp/v1` contract — ADR-050/Rule 6; endpoint chosen by INDEX into metadata, GET-only guard); `ConsoleAdminRegistrar` (thin hook registrar — `admin_menu`/`admin_enqueue_scripts`/`wp_ajax_*`, no-op outside WP). ADR-053 seam enforced by a reflection test asserting no UI/Admin ctor receives `DatabaseConnectionInterface`, `OperationsQueryReader`, or a concrete provider — UI reads only via `OperationsService` + `SystemInformationProvider`/`ModuleInspector`. Static assets `resources/operations/console.{js,css}` — hand-authored, self-contained; minimal vanilla JS does polling only; no bundle, no build step, no `resources/` build config. Wiring: `OperationsServiceProvider` register()s UI/Admin bindings + boot()s the two pages, MVP nav, four core dashboard widgets, and static assets; `ContentServiceProvider::boot()` registers a module-owned Content-Projections widget over `ContentMetricsProvider::KEY` (Rule 5); `headless-sync.php` binds `ConsoleAdminRegistrar` in normal admin requests. Additive service methods only (no OPSC-S1/S2 contract/registry/provider/reader changed): `OperationsService::endpointDescriptors()` + `RefreshCoordinator::endpointKeys()`. Test bootstrap gained WP escaping/admin/ajax/rest stubs (`esc_*`, `add_menu_page`, `wp_send_json_*`, `check_ajax_referer`, `rest_do_request`, `wp_enqueue_*`, etc.). Tests: 37 new Unit (HtmlTest; DashboardViewTest; PlaygroundViewTest; PlaygroundRequestExecutorTest; ConsoleAjaxControllerTest; AdminPageControllerTest; ConsoleUiWiringTest; NoInfrastructureReachableFromUiTest). Full Unit 940/940 green, 0 failures/warnings/deprecations (was 903 + 37), deterministic on two runs; Integration 0 failures with DB-dependent tests env-skipped (GATE-S4 git-clean guard skips without live PG, clears on commit — FLAG-OPSCS2-1). End-to-end render verified out-of-band: both pages render server-side with real snapshots; Playground lists all six `hsp/v1` endpoints and dispatches a live GET to `/hsp/v1/posts`. No state-changing action in the UI (OPSC-S4 scope); no migration/schema/new PG handle; git status shows only in-scope paths. Session close: committed `69d2d4a` → origin/main; clean-tree GATE-S4 rerun 3/3 green (28 assertions, live PG) — porcelain guard green → **FLAG-OPSCS2-1 RESOLVED**. **OPSC-S4 note (deferred nit):** `PlaygroundRequestExecutor` selects the endpoint by positional index into metadata captured at render time; a registration-order shift between render and execute resolves to a different endpoint. Bounded by the GET-only guard. Re-key by route string when touching this area in OPSC-S4. | no new flags; FLAG-OPSCS2-1 RESOLVED.

2026-07-15 | OPSC-S0 | Doc 12 Ratification (docs-only). Adopted Doc 12 (Admin Operations Console) into the frozen record via **DECISION V**, conservatively scoped per the architect's 2026-07-15 rulings. ARCHITECTURE_DECISIONS.md v1.19→v1.20: amendment-log row + DECISION V recording FLAG-PLANOPS1-1..11 verbatim in substance — (a/FLAG-3 B) server-rendered PHP + minimal vanilla JS + WP native admin, no node/npm/bundler, React deferred to a future ADR that must not alter provider/registry architecture; (b/FLAG-4 A) PSR-12 platform-wide + WPCS security at WP entry points only; (c/FLAG-5 A) console metrics derived on-demand (DECISION Q), zero persistence; (d/FLAG-6 A) Replay/Reconcile = thin delegators to ReplayService (T/S) + ReconciliationService (U), no second repair path, write-spy proof required in OPSC-S4 DoD; (e/FLAG-7 A) Flush Queue removed; (f/FLAG-8 C-modified) no Restart Workers — status/heartbeat/runbook links only; (g/FLAG-10 A) providers reuse delivery DatabaseConnectionInterface (K), four-handle topology (L Ruling 0) unchanged, no new pg_* (E); (h/FLAG-11 A) contracts under core/Contracts/Operations/ (Rule 5 verbatim); (i/FLAG-2 A) core/Operations/ lowercase added as core infra; (j) philosophy clause — observability/diagnostics only, not a control plane. FLAG-9 B: ratified ADR-047/048/049/050/052/053; ADR-051 HELD (not citable) pending FLAG-7/8 incorporation. Implications table +7 rows. Doc 11: added Phase 1A – Expanded (§4 list + new §6b). Doc 12: Draft → Accepted (as amended by DECISION V); §21 self-freeze removed; no other content edits (file already on disk without the (2) suffix — rename a no-op). CLAUDE.md: folder structure (core/Operations/ + core/Contracts/Operations/), Coding Standard TBD replaced with FLAG-4 A ruling, SETTLED gained DECISION V line. IMPLEMENTATION_PLAN.md §5b: inserted OPSC-S0..S4 rows (no BLOCKED markers; OPSC-S3 server-rendered PHP no resources/build config; OPSC-S4 Replay+Reconcile only). DOC12-ADOPTION-AUDIT.md: FLAG-PLANOPS1-1..11 marked RESOLVED. STATUS pointer → OPSC-S1. Docs-only — no production code, migrations, or tests. | no new flags; FLAG-PLANOPS1-1..11 all RESOLVED (DECISION V).

2026-07-14 | GATE-S4 | Architecture Validation Gate — Extensibility Validation PASSED (evidence-only gate session, no production code changed); the four-part Architecture Validation Gate is now COMPLETE. New test `tests/Integration/Gate/ExtensibilityValidationTest.php` (3 tests, 28 assertions, live PG 127.0.0.1:5432) proves all three §4 Extensibility criteria. Each test proves the POSITIVE half against the real runtime AND asserts the NEGATIVE half mechanically with `git status --porcelain -- <path>` (reports both tracked modifications and new untracked files; empty = untouched). Guard proven to have teeth: temporarily appending a comment to `core/Contracts/CanonicalModelInterface.php` made criterion 1 fail with the exact porcelain line, then reverted clean. (1) New content field without modifying `core/` — `test_criterion1_a_new_content_field_reaches_the_projection_without_touching_core`: a module-scoped `ContentFieldExtendingTransformer` adds a `reading_time` field via the EXISTING `CanonicalPost.meta` map; the REAL `PostAdapter` carries it into live `content.posts.meta_jsonb` (real DECISION 3 write path) with zero core AND zero adapter edits; asserts zero changes under `headless-sync/core/`. FINDING (not a defect): the real `PostAdapter::persist()` type-checks `instanceof CanonicalPost`, so a new-canonical-subclass approach is rejected at runtime — the true "add a content field" seam is the `meta` map on the existing `CanonicalPost`, which the test uses (first attempt used a new canonical class and errored; corrected). (2) New projection column without modifying transformer/canonical model — `test_criterion2_...`: drives the REAL, UNMODIFIED `PostTransformer` → `CanonicalPost` (asserted `::class` identity, no subclass), then a test-scoped `ProjectionColumnExtendingAdapter` derives a NEW real column `word_count` from the unmodified canonical output and writes a throwaway `content.posts_ext` table on live PG; asserts zero changes to `PostTransformer.php` + `CanonicalPost.php`. (3) New API resource without modifying existing endpoints — `test_criterion3_...`: a new `AuthorQueryProvider` + `AuthorResource` implementing the EXISTING core `QueryProviderInterface`/`ResourceInterface` (asserted `instanceof`) serve a live `content.authors` table (listing envelope data/next_cursor, single-item findBySlug, self-shaped fields); asserts zero changes to `ContentRestRegistrar.php`, `Resources/`, `Queries/`. All additions test-scoped throwaways (test schemas + doubles in the test file) — the extension SEAM is proven, not a Phase 1B feature. Combined gate suite 19/19 green (201 assertions), deterministic on two consecutive cold runs; full suite 932 tests, 2263 assertions, 0 failures, 0 deprecations (was 929 + 3 GATE-S4). Tests-only; only `tests/Integration/Gate/` + `STATUS.md` touched — no production code, no migrations, no contracts, no new event types. | no new flags. GATE-S4 DoD met; Extensibility gate done; **Architecture Validation Gate COMPLETE (all four sub-gates PASS)**. Phase 2 (WooCommerce) is unblocked by the gate but is OUT of MVP scope — awaits an explicit architect scope decision; no next session in the current Session Map.
2026-07-14 | maintenance | Deprecation cleanup (tests-only). Fixed the sole PHPUnit deprecation carried in every session log since P0-S5. Root cause: `tests/Unit/Queue/DatabaseQueueProviderTest.php::test_valid_partitions_are_accepted` bound its data provider via a doc-comment annotation `/** @dataProvider validPartitionProvider */`; PHPUnit 11.5 emits "Test Runner Triggered Deprecation (Metadata found in doc-comment … Metadata in doc-comments is deprecated and will no longer be supported in PHPUnit 12. Update your test code to use attributes instead.)". Fix: replaced with the `#[DataProvider('validPartitionProvider')]` attribute and added `use PHPUnit\Framework\Attributes\DataProvider;`. Behavior-preserving — same provider, same three rows (content/commerce/system), test still 3/3. Verified sole occurrence in the tree (grep `@dataProvider` across `tests/` = 1 hit, now 0). Full Unit suite 807/807 green, **0 PHPUnit deprecations** (previously 807 green + 1 deprecation, yellow banner). Only `tests/Unit/Queue/DatabaseQueueProviderTest.php` + `STATUS.md` touched — no production code, no contract, no migration. Suite now clean for the PHPUnit 12 upgrade. | no new flags.
2026-07-14 | GATE-S3 | Architecture Validation Gate — Operability Validation PASSED (evidence-only gate session, no production code changed). New test `tests/Integration/Gate/OperabilityValidationTest.php` (6 tests, 52 assertions, live MySQL 127.0.0.1:10053 + live PG 127.0.0.1:5432) proves all three §4 Operability criteria with real state (no `markTestIncomplete`): (1) Worker health / heartbeat-cycle crash detection (DECISION P) — `test_criterion1_worker_health_is_visible_and_a_crash_is_detected_within_one_heartbeat_cycle` drives the REAL `WorkerEngine.tick()` + `DatabaseHeartbeatPublisher` to upsert one current-state row per worker into `system.worker_heartbeats` (health visible: worker_id/worker_type/status, single row, no history); a monitor `last_heartbeat_at`-age query flags a crashed worker (beat older than one cycle) within one cycle while a freshly-ticked worker stays unflagged; derived `worker_count` (DECISION Q) confirms visibility. (2) DLQ diagnostics (OPEN-3) — `test_criterion2_dead_lettered_job_carries_payload_snapshot_and_stack_trace` drives a job to ADR-022 retry-limit exhaustion through the real `EventWorkerStrategy` (relay+dispatch from live MySQL), then reads the DLQ row back via `DeadLetterRepository::inspect` (`hsp dlq inspect`) — asserts a non-empty real PHP stack_trace (frame markers) and a NOT-NULL structured JSONB payload_snapshot (DECISION A self-contained/replayable) + failure_reason; no projection written. (3) Reconciliation executes + WordPress wins (ADR-026/027/045, DECISION U) — four tests covering hourly drift (missed capture), nightly incremental (checksum-drift rename invisible to existence-only hourly — D2), weekly full (PG→WP orphan sweep → `.deleted` → DECISION I tombstone), plus idempotency + in-flight (D4) suppression; every repair is DECISION T re-emission ONLY, and a `WriteSpyConnection` proves `executeCount==0` and zero write-txns on the repair path in EVERY mode, so WordPress-wins holds by construction. Reused OPS-S3 reconciliation helpers (StoreReconciliationSource, WriteSpyConnection) + replay fakes (FakeWpStore/FakeWpdb/ReplayReadingLoader) and the real emit path (ReconciliationService→ReplayService→ContentReplayEmitter→OutboxWriter). Fixed a bad hex digit in a hand-written crashed-worker UUID before green. GATE-S3 suite 6/6 green; combined gate suite 16/16 green (173 assertions), deterministic on two consecutive cold runs; full suite 929 tests, 2235 assertions, 0 failures (was 923 + 6 GATE-S3), sole deprecation is the pre-existing P0-S5 Unit-suite carry (not from this change). Tests-only; only `tests/Integration/Gate/` + `STATUS.md` touched — no production code, no migrations, no contracts. | no new flags. FLAG-GATES3-1 RESOLVED (DECISION U built in OPS-S3, validated here). GATE-S3 DoD met; Operability gate done. Pointer → GATE-S4 (Extensibility).
2026-07-13 | OPS-S3 (build) | Reconciliation MVP SHIPPED (DECISION U v1.18→v1.19; FLAG-GATES3-1 resolved). STEP 2a: amended DECISION U with the three ratified design decisions (v1.19 — D1 comparison signal by mode [hourly timestamp/existence, nightly window+checksum, weekly full+checksum+orphan]; D2 taxonomy existence-only [terms have no modified ts; field drift caught by checksum]; D3 direction-by-mode [hourly+nightly WP→PG; orphan sweep PG→WP full-only; missed-delete latency ≤ weekly]; D4 suppression [pending unrelayed outbox OR system.events version > latest_processed_version]; D5 executor B1; D6 core `WpReconciliationSourceInterface`; D7 unbounded paged full sweep, page size config-driven) + Implications rows. STEP 2b pre-check PASS: verified `content.*.updated_at` is source-derived (`post_modified_gmt` → extractor `modifiedAt` → adapter `$updatedAt`), NOT processing time (`synced_at`/`created_at` bind wall-clock separately) — comparator sound. STEP 2c build — core: `core/Contracts/WpReconciliationSourceInterface` + `SourceState`; `core/Reconciliation/ReconciliationService` (detector + batching + D4 suppression + 3 modes as one detector+mode param; repair via `ReplayService::replayEntity` ONLY, no direct PG writes) + `ReconciliationResult` + `ReconciliationCronRegistrar` (WP-Cron triggers, custom nightly/weekly intervals, cadence config-driven); `core/Cli/ReconcileCommand` (+ `reconcile` DECISION Q counter) + `WpCliReconcileRegistrar` (`hsp reconcile drift|incremental|full|status`); un-stubbed `ReconciliationWorkerStrategy` as façade (reconcileDrift/Incremental/Full; execute() producer-side no-op per B1). Module: `modules/Content/Reconciliation/WpReconciliationSource` (WP reads via WpContentLoader + $wpdb ID paging + checksum recompute reusing real extractor/transformer pipeline + pending-outbox read). Config: `config/worker.php` reconciliation block (schedules + page_size). DI: ContentServiceProvider binds the source; WorkerServiceProvider binds service/strategy/command/cron (constructor injection, ADR-012); headless-sync.php registers CLI (WP-CLI guard) + cron (plugins_loaded). Tests: 25 unit (ReconciliationServiceTest — missed create/update, checksum drift, orphan, tombstoned-ok, equal-ts non-drift, both suppression paths, dry-run, paging, taxonomy existence-only, no-direct-PG-write; ReconciliationWorkerStrategyTest — delegation + execute() no-op; ReconcileCommandTest — counter/routing/dry-run) + 6 integration live MySQL(10053)+PG(5432) (missed-capture repair→converge; captured-not-relayed + relayed-not-processed suppression; orphan→tombstone via re-emit; category rename caught by checksum not hourly; WordPress-wins out-of-band mutation repaired via pipeline with WriteSpyConnection proving executeCount==0). Full suite 923 tests, 2183 assertions, 0 failures, 1 pre-existing P0-S5 deprecation; deterministic on two cold runs. Container smoke: all 5 reconciliation bindings resolve (module source behind core contract — Rule 5). Verified: no core→module import, no new pg_* wrapper / no fifth handle (DECISION E/L Ruling 0), no schema change, ReconciliationService issues no DML. | FLAG-GATES3-1 RESOLVED (DECISION U, built + validated). No new flags. GATE-S4 does NOT begin; next = GATE-S3.
2026-07-13 | OPS-S3 (STEP 0 — ruling recorded; STEP 1 design pending review) | Recorded architect ratification of FLAG-GATES3-1 **Option A** as **DECISION U** (ARCHITECTURE_DECISIONS.md v1.18). DECISION U: inserts new session **OPS-S3 "Reconciliation MVP"** immediately before GATE-S3; **GATE-S3 Depends-on → OPS-S3; GATE-S3 DoD UNCHANGED**. Repair mechanism = DECISION T re-emission (`ReplayService`/`ReplayEmitterInterface`) **ONLY** — direct PG projection writes PROHIBITED; WordPress-wins by construction (ADR-026/027/045). Scope = drift detection + incremental validation + full reconciliation, detecting (a) missed captures (WP newer/absent vs delivery — DECISION 1 backstop) and (b) orphans (present in delivery, deleted/non-public in WP → re-emit `.deleted` → DECISION I tombstone). Scheduling = WP-Cron authorized to *trigger* only (recovery-jobs carve-out); workers remain the execution path. Constraints: no fifth PG handle (DECISION L Ruling 0), no new `pg_*` wrapper (DECISION E), no new event contracts, Rule 5 module isolation. Docs touched: ARCHITECTURE_DECISIONS.md (v1.17→v1.18 — amendment log, DECISION U body, two Implications rows); IMPLEMENTATION_PLAN.md §5b (OPS-S3 row inserted, GATE-S3 Depends-on GATE-S2→OPS-S3, GATE-S3 Authority notes DECISION U); STATUS.md (header, Next session→OPS-S3, gate checklist Operability→PENDING OPS-S3, FLAG-GATES3-1→RESOLVED, this log line). NO production code, migrations, or contracts touched. **STEP 1 design note proposed and STOPPED for architect review — no reconciliation code until the design is ratified in-chat** (DECISION U point 6). | FLAG-GATES3-1 RESOLVED (DECISION U). No new flags.
2026-07-13 | GATE-S3 (pre-implementation) | Architecture Validation Gate — Operability Validation ATTEMPTED, STOP-and-flag. Read STATUS.md, IMPLEMENTATION_PLAN.md §4 (Operability Validation) + §5b GATE-S3 row, ARCHITECTURE_DECISIONS.md (DECISION P heartbeat, OPEN-3 DLQ context, DECISION T line 792, ADR-026/027/045), Doc 4 §886 (ADR-026 schedule). Audited the production code each criterion must validate: criterion 1 (worker health / failure detection within one heartbeat cycle — DECISION P) backed by real code (DatabaseHeartbeatPublisher/HeartbeatRecord/`system.worker_heartbeats`) — validatable; criterion 2 (DLQ payload snapshot + stack trace — OPEN-3) backed by real code (DeadLetterRepository/`system.dead_letter_jobs`/DlqCommand) — validatable; criterion 3 (Reconciliation — ADR-026/027/045) NOT validatable — `ReconciliationWorkerStrategy::execute()` is a STUB returning `false` ([ReconciliationWorkerStrategy.php:23](headless-sync/core/Workers/Strategies/ReconciliationWorkerStrategy.php#L23)), registered as a stub in the DI container ([WorkerServiceProvider.php:113](headless-sync/core/Container/Definitions/WorkerServiceProvider.php#L113)); full-codebase search found NO drift detection, NO incremental validation, NO full reconciliation, NO WordPress-wins repair, NO `hsp reconcile` CLI command, NO schedule/cron wiring. Reconciliation was never built (OPS-S1 shipped heartbeat/DLQ/replay/metrics only; Phase 3 Operational Hardening, where it is deferred, is post-MVP; DECISION T itself labels reconciliation "future" work that reconciliation sessions will build on). Per the GATE-S3 brief (NO production code changes; a failed validation check is a STOP-and-flag, not a fix; DoD forbids `markTestIncomplete`), building reconciliation is out of scope and writing evidence against absent behavior is disallowed. Root cause: Session-Map sequencing conflict — GATE-S3 requires reconciliation to *execute* before any session builds it. STOPPED before writing any gate test. NO production code changed, NO gate test written, NO migration/contract touched. STATUS.md only: header, gate checklist (Operability → BLOCKED), Next-session pointer held at GATE-S3 (NOT advanced to GATE-S4), and FLAG-GATES3-1 recorded with three ruling options (A: insert a reconciliation build session before GATE-S3; B: descope criterion 3 to Phase 3, pass GATE-S3 on criteria 1+2; C: ratify a reduced-scope reconciliation build for the gate). | **Raised FLAG-GATES3-1** (CRITICAL STOP CONDITION — reconciliation is a stub, never built; GATE-S3 criterion 3 unsatisfiable; blocks GATE-S3 completion; architect ruling required). GATE-S4 does NOT begin.
2026-07-13 | GATE-S2 | Architecture Validation Gate — Scalability Validation PASSED (evidence-only gate session, no production code changed). New test `tests/Integration/Gate/ScalabilityValidationTest.php` (4 tests, 65 assertions, live MySQL 127.0.0.1:10053 + live PG 127.0.0.1:5432) proves all three §4 Scalability criteria with real state (no `markTestIncomplete`): (1) SKIP LOCKED verified — `test_criterion1a` opens two independent physical PG sessions (`PGSQL_CONNECT_FORCE_NEW`), each HOLDS a `FOR UPDATE` lock on a queue row at the same instant; session B runs the exact OPEN-4 claim SELECT while A holds its lock and gets a DIFFERENT row, under a 2 s `statement_timeout` so blocking-on-A's-row would raise `lock_timeout` instead of hanging (deterministic "no claimant blocks" proof); a third session skips BOTH locked rows (two claims genuinely concurrent). `test_criterion1b` drives the REAL `DatabaseQueueProvider::claim()` from 3 workers on 3 physical connections over 12 jobs round-robin → every job claimed exactly once, claimed-set == whole queue, ≥2 workers participate, PG shows N rows claimed once each (no collision). (2) Head-blocking — `test_criterion2` has worker A claim the HEAD job (earliest `available_at`) with a 10 h visibility timeout so it is stuck; worker B claims + COMPLETES all 6 followers past it; B never touches the head; head stays `claimed` by A. (3) Replay under load — `test_criterion3` fires `ReplayService::replayEntity` (DECISION T) for a corrupted aggregate group WHILE a mid-flight normal-load batch (4 posts + 2 pages) is in the same outbox→relay→dispatch→queue, all drained interleaved in one worker loop; asserts both groups converge to correct final projections, `aggregate_versions` strictly monotonic (replay advanced via fresh DECISION 2 counter; normal at v1; none regressed), one projection row per aggregate, zero duplicate `system.processed_events` (PK-keyed; distinct == total). Reused GATE-S1/OPS-S2 replay fakes (FakeWpStore/FakeWpdb/ReplayReadingLoader) and the real emit path (ReplayService→ContentReplayEmitter→OutboxWriter). Deterministic teardown (unique `test_gate_s2_` prefix, DROP SCHEMA CASCADE, `$pgConn = null` after pg_close, hand-driven extra sessions rolled-back+closed). GATE-S2 suite 4/4 green; combined gate suite 10/10 green (121 assertions), verified deterministic on immediate back-to-back re-run (no cross-run residue); full suite 892 tests, 2091 assertions, 0 failures, 1 pre-existing PHPUnit deprecation (P0-S5 carry, not from this change). Tests-only; only `tests/Integration/Gate/` + `STATUS.md` touched — no production code, no migrations, no contracts. | no new flags. GATE-S2 DoD met; Scalability gate done. Pointer → GATE-S3 (Operability).
2026-07-12 | GATE-S1 (resume) | Reliability Validation PASSED — criterion 2 re-run only. Replaced the STOP-and-flag `markTestIncomplete` in `tests/Integration/Gate/ReliabilityValidationTest.php` with real gate evidence for all three replay modes (Doc 4 §24; §4 criterion): `test_criterion2_single_event_replay_succeeds` (DECISION S single-DLQ-id entry-point shape; exercised end-to-end by criterion 3), `test_criterion2_entity_replay_converges_a_corrupt_projection_to_correct_final_state` (corrupt slug+checksum → shipped DECISION T `ReplayService::replayEntity` → synthetic re-emission through the real outbox→relay→dispatch→worker pipeline → projection converges to current WP state, no duplicate row, not tombstoned), and `test_criterion2_date_range_replay_converges_in_window_aggregates_and_leaves_out_of_window_untouched` (two in-window aggregates corrupted → `replayRange` over half-open [from,to) → both converge; out-of-window aggregate untouched, checksum + version unchanged). Reused the OPS-S2 replay fakes (FakeWpStore/FakeWpdb/ReplayReadingLoader) and DECISION T front-end (ReplayService→ContentReplayEmitter→OutboxWriter); added the `wp_hsp_aggregate_counters` table + a `system.events` history seeder to the gate harness. Criteria 1 & 3 untouched (already PASS). Gate suite 6/6 green, 0 Incomplete; full suite 888 tests, 2026 assertions, 0 failures, 1 pre-existing PHPUnit deprecation (P0-S5 carry, not from this change). Tests-only change; only `tests/Integration/Gate/` + `STATUS.md` touched — no production code. | FLAG-GATES1-1 RESOLVED (DECISION T + gate re-run). GATE-S1 DoD met; Reliability gate done. Pointer → GATE-S2 (Scalability).
2026-07-12 | OPS-S2 | Replay Engine SHIPPED — entity + date-range replay (DECISION T, ARCHITECTURE_DECISIONS.md v1.17; FLAG-OPSS2-1 Option A ratified). Step 0: recorded DECISION T (replay = projection repair via synthetic re-emission — new event through wp_hsp_outbox with a fresh wp_hsp_aggregate_counters version [DECISION 2], flows relay→dispatch→worker, passes the DECISION J stale guard NATURALLY [new version > stored; guard not weakened/bypassed/conditional]; historical system.events never mutated or re-enqueued [Doc 5 §26 holds — replay appends]; emitter reads CURRENT WP state [ADR-044/045]: exists+public→.updated, missing/non-public→.deleted [tombstones outage-window deletions]; date-range = SELECT DISTINCT (aggregate_type,aggregate_id) FROM system.events in [from,to) via the EXISTING delivery handle [no fifth handle, DECISION L Ruling 0]; correlation_id per run + causation_id per op; convergence primitive future reconciliation builds on). Amendment log + Implications rows added; FLAG-OPSS2-1 marked resolved. Build: core/Contracts/ReplayEmitterInterface (core-owned; module implements — Rule 5); core/Replay/ReplayService + ReplayResult (entity + date-range orchestration, DISTINCT discovery, traceability, no projection writes); modules/Content/Replay/ContentReplayEmitter (WP-state read → .updated/.deleted → emit via existing EventProvider/OutboxWriter path); ReplayWorkerStrategy un-stubbed (replayEntity()/replayRange() → ReplayService; execute() stays no-op — producer-side, not a system-queue consumer); WP-CLI core/Cli/ReplayCommand + WpCliReplayRegistrar (`hsp replay entity <type> <id>` / `hsp replay range <from> <to>`, emits DECISION Q `replay` structured counter); DI wired (ContentServiceProvider binds ReplayEmitterInterface + ReplayService; WorkerServiceProvider binds worker.strategy.replay + ReplayCommand; headless-sync.php registers the replay CLI under WP_CLI guard). Tests: 29 new unit (ContentReplayEmitter decision matrix incl. missing/non-public→deleted + traceability; ReplayService entity/date-range/half-open-bounds/distinct/unknown-type/read-only; ReplayCommand counter emission + range validation; ReplayWorkerStrategy delegation + execute() no-op) + ReplayEngineIntegrationTest ×6 live MySQL+PG (entity reproject, deleted→tombstone, date-range incl. 'from'-boundary + since-deleted aggregate + out-of-window exclusion, idempotency + strict version advance, synthetic version > stored passes DECISION J guard, historical system.events immutability). ReplayWorkerStrategyTest also proves execute() idles cleanly under a real WorkerEngine (5 ticks → all no-work + 'idle' heartbeats + zero connection I/O — no busy-spin/queue-claim/exception; DECISION T producer-side strategy; Doc 8 roster note recorded in DECISION T Implications, Doc 8 NOT edited). Full suite: 886 tests, 2014 assertions, 0 failures, 0 errors, 1 pre-existing PHPUnit deprecation (P0-S5 carry), 1 Incomplete (GATE-S1 criterion 2 STOP-and-flag — out of OPS-S2 scope; flips PASS on GATE-S1 resume). Verified: no new pg_* wrapper, no fifth PG handle (ReplayService reuses the delivery DatabaseConnectionInterface), core never imports a module. | FLAG-OPSS2-1 RESOLVED (DECISION T). FLAG-GATES1-1 UNBLOCKED (entity + date-range replay shipped; GATE-S1 criterion 2 re-run only remains). Pointer → GATE-S1 resume (criterion 2 re-run).
2026-07-12 | OPS-S2 (pre-implementation) | Opened Replay Engine session; inserted OPS-S2 row in IMPLEMENTATION_PLAN.md §5b (after OPS-S1) and amended GATE-S1 Depends-on → "OPS-S1, OPS-S2". STOPPED before writing any replay code — hit CRITICAL STOP CONDITION: Doc 4 §24 does not specify how entity/date-range replay of already-processed aggregates interacts with the binding DECISION J stale guard (`version <= stored → ack, zero writes` suppresses reprojection). | **Raised FLAG-OPSS2-1** (architect ruling required: choose reprojection mechanism — Option A fresh synthetic event / B guard bypass / C version-state reset — and authorize any schema change). No code written; DoD not met by design (blocked on ruling).
2026-07-12 | GATE-S1 | Architecture Validation Gate — Reliability Validation (evidence-only gate session, no production code changed). IMPLEMENTATION_PLAN.md §5b: added GATE-S1..GATE-S4 rows (Reliability/Scalability/Operability/Extensibility, criteria copied verbatim from §4; GATE-S1 depends-on OPS-S1, each subsequent gate depends on the prior); §4 not altered. New test tests/Integration/Gate/ReliabilityValidationTest.php (4 tests, live MySQL 127.0.0.1:10053 + live PG 127.0.0.1:5432): criterion 1 (successful sync under normal load) PASS — proven end-to-end by assembling the REAL runtime pipeline (RelayWorkerStrategy → EventDispatcher → EventWorkerStrategy → ContentSubscriber → Page/Post/CategoryAdapter) over a 12-aggregate mixed batch (6 posts + 4 pages + 2 categories): outbox → relay → system.events → dispatch → system.queue_jobs → worker drain → content.* projection, with completed/aggregate_versions/processed_events all correct and zero dead-letters; plus an update-reprojection test (version advance, idempotent upsert). Criterion 3 (DLQ recovery to correct final state) PASS — retry-limit exhaustion → system.dead_letter_jobs with full OPEN-3 context (stack_trace, payload_snapshot) → DeadLetterRepository::replay (the `hsp dlq replay` path, DECISION S lifecycle) → healthy worker drives replayed job to correct final content.posts projection → replayed_at stamped, DLQ row retained. Criterion 2 (single + entity + date-range replay) FAIL/BLOCKED — test_criterion2 proves single-event replay present and entity + date-range modes ABSENT (ReplayWorkerStrategy stub; DeadLetterRepository/DlqCommand single-DLQ-id only), marked Incomplete = STOP-and-flag per gate brief + CLAUDE.md freeze rule. Only substitution on the pipeline is GateReloadingLoader (WP state-reload boundary, DECISION H, ADR-044) — relay/dispatch/worker/adapters/PG are the real components. STATUS.md: gate checklist added with per-criterion PASS/FAIL + named tests; Reliability item marked BLOCKED (not done — DoD unmet). Full suite: 852 tests, 1908 assertions, 0 failures, 0 errors, 1 pre-existing PHPUnit deprecation (P0-S5 carry), 1 intentional Incomplete (GATE-S1 criterion 2). | FLAG-GATES1-1 raised (entity + date-range replay not implemented — GATE-S1 criterion 2 fails; STOP-and-flag; blocks the Architecture Validation Gate; needs an authorized non-gate session to build entity + date-range replay per Doc 4 §24). GATE-S2 does NOT begin until resolved.
2026-07-12 | OPS-S1 | Early Operational Baseline shipped. (1) Migrations: 0012_create_system_worker_heartbeats.sql (DECISION P frozen DDL — single current-state table, worker_id UUID PK, worker_type/status TEXT, last_heartbeat_at/started_at TIMESTAMPTZ) + 0013_add_replayed_at_to_dead_letter_jobs.sql (DECISION S clause (e) — replayed_at TIMESTAMPTZ NULL forward-add; 0004 untouched); both registered in MigrationServiceProvider migrations.core; applied + idempotency-proven on live PG (hsp DB), column shapes verified. (2) DatabaseHeartbeatPublisher (implements existing HeartbeatPublisherInterface; ctor-injected connection = worker-runtime handle 'queue.connection.pgsql' per DECISION L Ruling 0 — no new handle/class/pg_* wrapper; INSERT…ON CONFLICT(worker_id) DO UPDATE upsert per tick, µs-precision TIMESTAMPTZ so advances are visible); HeartbeatRecord extended with worker_type/started_at (defaulted, back-compat); WorkerEngine carries workerType + startedAt; WorkerServiceProvider swaps NullHeartbeatPublisher→DatabaseHeartbeatPublisher on runtime path. (3) MaintenanceWorkerStrategy un-stubbed — drives requeueTimedOut() per partition on a config-driven cadence (config/worker.php maintenance.recovery_interval_seconds default 30; no hardcoded timing at call site; 'worker' added to ConfigLoader CONFIG_FILES); worker.engine.maintenance bound. (4) Derived-metrics surface OperationalMetricsQuery (queue depth total+per-partition, DLQ depth, oldest-pending age, worker count — on-demand SQL aggregates, no metrics table, DECISION Q) + WorkerCounters (processed/retry/failure/replay in-process) + StructuredLogger (JSON metric lines, error_log sink) emitted by WorkerEngine on worked ticks; EventWorkerStrategy increments counters at complete/release/deadLetter; a successful WP-CLI `hsp dlq replay` emits the `replay` counter as a structured `dlq.replay` log line (event_id + ts) from DlqCommand (StructuredLogger ctor-injected, ADR-012) — replay runs outside the WorkerEngine tick loop so the emission lives there. (5) DLQ read/replay: DeadLetterRepository (list/inspect/replay — single-PG-txn lifecycle verify-exists FOR UPDATE → verify replayed_at IS NULL → DELETE queue row by event_id → INSERT fresh job attempts=0 → stamp replayed_at; DLQ rows never deleted) + DeadLetterReplayException; WP-CLI DlqCommand + WpCliDlqRegistrar ('hsp dlq list|inspect|replay', 'hsp status'), registered in headless-sync.php under WP_CLI guard (in-scope CLI registration). Tests: 19 new unit (MaintenanceWorkerStrategy cadence, DatabaseHeartbeatPublisher SQL, DeadLetterRepository lifecycle+guards, DlqCommand incl. replay-emits-structured-log + failed-replay-emits-nothing, WorkerCounters/StructuredLogger) + OperationalBaselineIntegrationTest (7 live-PG: DLQ populate w/ OPEN-3 context, replay→fresh-claimable-job through UNIQUE(event_id) + naive-no-op trap proven + double-replay rejected, stale replay = ack+zero writes, heartbeat visible+advances per tick, crash→visibility-timeout→requeue THROUGH real WorkerEngine+MaintenanceWorkerStrategy runtime driver, derived metrics, structured counter emission). Full suite: 848 tests, 1864 assertions, 0 failures, 0 skipped (MySQL 127.0.0.1:10053 + PG 127.0.0.1:5432 live), 1 pre-existing PHPUnit deprecation carried from P0-S5. ADR-012 clean (no service-locator in new business logic); DECISION 3 atomicity untouched; four-connection topology unchanged. | no new flags. FLAG-OPSS1-1..4 + FLAG-P1AS6D-1 remain Resolved (built to DECISIONS P/Q/R/S + Ruling 0).
2026-07-11 | housekeeping | Recorded architect's 2026-07-11 OPS-S1 rulings; flags resolved. ARCHITECTURE_DECISIONS.md bumped to v1.16: DECISION L amended with Ruling 0 (four-connection topology FROZEN as final — relay/queue-worker/delivery/dispatcher; no fifth handle without new ADR; heartbeat on existing worker-runtime handle); new DECISION P (Worker Heartbeat Storage — single current-state `system.worker_heartbeats` table, upsert per tick, DatabaseHeartbeatPublisher via existing interface + ctor injection, migration authorized); DECISION Q (Metrics Without Persistence — no metrics table; derived-on-demand + structured logs; "metrics emit" DoD defined); DECISION R (Visibility-Timeout Recovery Driver — MaintenanceWorkerStrategy drives requeueTimedOut(), config-driven cadence); DECISION S (DLQ Replay Lifecycle — permanent audit rows, one-txn verify→delete-queue-row→insert-attempts-0→stamp replayed_at, passes DECISION J guard, WP-CLI only; `replayed_at` confirmed absent from migration 0004, forward-migration add authorized). Implications table updated (system.worker_heartbeats, dead_letter_jobs.replayed_at, PHP contracts rows). STATUS.md: FLAG-P1AS6D-1 + FLAG-OPSS1-1..4 marked Resolved with ruling refs; OPS-S1 pointer unblocked. IMPLEMENTATION_PLAN.md §5b OPS-S1 row: added DECISION P/Q/R/S refs to Authority; widened migration clause. NO code, NO migrations written. | flags resolved: FLAG-P1AS6D-1, FLAG-OPSS1-1, FLAG-OPSS1-2, FLAG-OPSS1-3, FLAG-OPSS1-4.
2026-07-11 | OPS-S1 (pre-implementation review) | No code written. Read STATUS.md, IMPLEMENTATION_PLAN.md §5b OPS-S1 row, ARCHITECTURE_DECISIONS.md OPEN-3/OPEN-4/DECISION E/K/L, Doc 8 §15/§27, Doc 4 §24. Audited existing surface: DLQ schema (0004) complete with all four OPEN-3 columns; DatabaseQueueProvider has deadLetter()/requeueTimedOut()/enqueueIdempotent(); EventWorkerStrategy dead-letters at attempts>=retryLimit; ReplayWorkerStrategy + MaintenanceWorkerStrategy are stubs; HeartbeatPublisher is NullHeartbeatPublisher (discards); no heartbeat/metrics table exists in frozen schema or Implications table; no metric counters produced anywhere; nothing invokes requeueTimedOut() on a runtime loop. STOPPED before implementation and recorded all ruling-needed conflicts as open flags per instruction. | FLAG-OPSS1-1 (heartbeat/metrics table never frozen — blocks heartbeat+metrics DoD; needs OPEN-N-style DDL freeze + connection-binding ruling, entangled with frozen topology / FLAG-P1AS6D-1); FLAG-OPSS1-2 (metric counters have no source of truth); FLAG-OPSS1-3 (crash→visibility-timeout requeue has no runtime driver — MaintenanceWorkerStrategy stub); FLAG-OPSS1-4 (replay entry-point + DLQ-inspect surface unspecified; UNIQUE(event_id) on queue_jobs makes naive re-enqueue a silent no-op unless prior job row cleared — must be ruled).
2026-06-25 | P1A-S8 | shipped (DECISION O v1.15 credential resolution: define→getenv→default, fail-loud, MySQL inherits WP DB_*; bootstrap/CredentialResolver + 25 unit tests; four runtime providers + MigrationServiceProvider rewired to resolver via constructor injection; Environment::overrides() DB keys removed; wp-config define()s; FLAG-P1AS8-1 found+fixed at source — dbname/name mismatch, migration engine now connects live; topology + test injection unchanged; 822 tests + smoke 39/39 + live migration connect green) | FLAG-P1AS8-1 closed; FLAG-P1AS6D-1 open (unchanged); no new flags.
2026-06-25 | P1A-S7 | REST namespace rename: api/v1 → hsp/v1. DECISION N recorded (ARCHITECTURE_DECISIONS.md v1.14). ContentRestRegistrar::NAMESPACE = 'hsp/v1'; all six register_rest_route() calls reference constant. hsp-blog/lib/api.ts: six fetch paths updated. smoke_e2e.php: four curl/echo strings updated. Doc reconciliation: DECISION F Implements table, IMPLEMENTATION_PLAN.md §4 endpoint bullets + pipeline diagram + P1A-S5/S6a session rows, Phase 1A DoD Next.js bullets, docs/09-delivery-api-and-consumption-architecture.md §7/§17 examples, FLAG-P1AS5-1 resolved. P1A-S7 + P1A-S8 rows inserted in Session Map; OPS-S1 Depends-on updated to P1A-S8. PHPUnit: 710/710 unit tests pass, 0 failures, 1 pre-existing deprecation. grep api/v1 in modules/, tests/, hsp-blog/, tools/ = 0 hits; docs/ = 4 meta-references in DECISION N text itself (Supersedes/Rationale/amendment log) + P1A-S7 session row name — all intentional. | FLAG-P1AS5-1 resolved.
2026-06-25 | P1A-S6 | shipped (live PG migration apply 16 migrations; smoke_e2e.php dispatcher-tick + grep/BOM/replay fixes; 39/39 smoke; 797/797 tests; DoD-4 via AdapterAtomicityIntegrationTest 11/11, DoD-5 worker-level via HandlerSpineIntegrationTest, DoD-2 inline-tick latency ~50–160ms, production loop latency deferred to OPS-S1) | flags: FLAG-P1AS6D-1 open (carry-over).
2026-06-24 | P1A-S6a (review pass) | ADR-012 fix for FLAG-P1AS6A-5: replaced bare \Closure capturing Container with typed ContentRestRegistrarFactory (final class, modules/Content/Rest/ContentRestRegistrarFactory.php). Factory receives six per-dep \Closure factories from ContentServiceProvider; holds no Container reference; memoizes ContentRestRegistrar on first __invoke(). ContentModule::$restRegistrarFactory typed ContentRestRegistrarFactory — grep-clean of ContentModule.php for Container::get/global $container. Lazy deferral preserved: DatabaseConnectionInterface not resolved until rest_api_init fires, proven by testContentRestRegistrarIsNotConstructedAtModuleLoadTime (spy counter). ContentModuleBootTest gains 3 new assertions (no-Container-reference reflection test, typed-factory assertion, lazy-deferral spy). Suite: 734/734, 0 failures, 0 skipped, 1 pre-existing deprecation. Live WP: api/v1 ✓, 6 routes ✓. FLAG-P1AS6A-5 resolved. FLAG-P1AS6-2 resolution text updated. FLAG-P1AS6A-1 marked E2E-blocking. FLAG-P1AS6A-2/-3/-4 recorded-and-kept. | no new flags.
2026-06-24 | P1A-S6a | Bootstrap/DI fix (FLAG-P1AS6-2 Gaps A/B/C resolved). Shipped: ContentServiceProvider (PageQueryProvider, PostQueryProvider, CategoryQueryProvider, PageResource, PostResource, CategoryResource, ContentRestRegistrar, HookWiring, EventProvider, ContentModule — all container-bound); ModuleLoader refactored to inject Container + resolve via explicit bindings first, fallback new $class() for zero-arg, throw for required-arg without binding (no reflection autowiring); Application::boot() now calls registerAll() after bootstrap(); ContentModule gains lazy \Closure ContentRestRegistrar factory dep + boot() wires add_action('rest_api_init') lazily (PG connection deferred to rest_api_init hook); headless-sync.php gains extension_loaded('pgsql') guard (graceful admin notice + bail); ModuleServiceProvider passes Container to ModuleLoader; ContainerBuilder registers ContentServiceProvider; DatabaseConnectionInterface binding added in QueueServiceProvider; \pg_connect() global prefix fixed in OutboxServiceProvider and QueueServiceProvider; wp-config.php HSP env vars added for local dev PG/MySQL credentials; php.ini php_pgsql.dll enabled in web server. Tests: 731 total (722 prior + 9 new: ModuleLoaderTest ×3 new, ContentModuleBootTest ×6), 0 failures, 0 skipped (with live DB env vars), 1 pre-existing deprecation. Live WP DoD: api/v1 present in wp-json namespaces ✓; all 6 content routes registered (/posts, /posts/{slug}, /pages, /pages/{slug}, /categories, /categories/{slug}) ✓; routes 500 on missing content.* schema (P1A-S4 migrations not applied to local Docker PG — expected; not a code bug). FLAG-P1AS6-2 resolved. Session Map updated: P1A-S6a + P1A-S6b inserted; P1A-S6 E2E now depends on P1A-S6b. | no new flags.
2026-06-24 | P1A-S6 (partial — flags block E2E DoD) | Next.js consumer app built and verified (hsp-blog/: lib/api.ts, app/posts/page.tsx, app/posts/[slug]/page.tsx, app/pages/[slug]/page.tsx, not-found.tsx; TypeScript clean; production build passes; HTTP 200 on all consumer routes against running server). Type-canon check PASS (all content.* migrations: TIMESTAMPTZ timestamps, VARCHAR(64) checksums). Module isolation check PASS (no cross-module imports, no service-locator calls in business logic). IMPLEMENTATION_PLAN.md §4 reconciled: added missing GET /api/v1/categories/{slug} bullet (FLAG-P1AS5-1 resolved). PHP test suite: 722/722. E2E DoD blocked by two flags requiring architect rulings. | flags: FLAG-P1AS6-2 (plugin bootstrap incomplete — Application::boot() never calls module.registrar::registerAll(); ModuleLoader uses new $class() without args; ContentModule has no rest_api_init wiring — REST routes never register); FLAG-P1AS6-1 (EventWorkerStrategy::executeHandler() is a P0-S6 stub — no Content Subscribers/Handlers exist; queue → PG projection pipeline is unimplemented).
2026-06-24 | P1A-S5 | REST Delivery API — 6 endpoints, core QueryProvider/Resource/FilterSet/CursorPage contracts (DECISION F v1.9), cursor pagination proven on live PG, status/cursor 400s, single-fetch publish+not-deleted 404 guard, limit clamps. Shipped: core/Contracts/ (QueryProviderInterface, ResourceInterface, FilterSet, CursorPage); modules/Content/Queries/ (PageQueryProvider, PostQueryProvider, CategoryQueryProvider — (sort,id) tiebreaker cursor, DEFAULT/MAX limits, projection-side category join); modules/Content/Resources/ (PageResource, PostResource, CategoryResource — contract fields only, no internal columns); modules/Content/Rest/ContentRestRegistrar (WP-only boundary: sanitize inputs, 400 non-public status, 400 malformed cursor, 404 missing/soft-deleted/non-publish slug, six /api/v1/ routes); tests/bootstrap.php (WP REST stubs). Tests: 664 unit + 58 integration = 722 total, 0 failures. Shared-sort-value cursor edge case proven against live PostgreSQL (pages/posts: shared published_at; categories: shared name). DECISION F recorded in ARCHITECTURE_DECISIONS.md v1.9. | flags: FLAG-P1AS5-1 (IMPLEMENTATION_PLAN.md §4 five-bullet undercount — categories/{slug} missing; Session Map authoritative; reconcile plan text).
2026-06-22 | P0-S1 | Shipped: headless-sync.php, bootstrap/ (Application, Bootstrapper, Environment, Constants, Version), config/ (7 skeletons), core/Container/ (Container, ContainerBuilder, ServiceRegistry, ServiceProvider, Definitions/CoreServiceProvider), core/Configuration/ConfigLoader, core/Psr/Container/ (PSR-11 stubs), composer.json (autoload only), vendor/autoload.php generated. Config hierarchy (Global→Module→Env), PSR-11 container, and ADR-012 constructor injection all verified via smoke tests. | FLAG-P0S1-1: PSR-11 stubs bundled locally (see flags section below)
2026-06-22 | housekeeping | Monorepo restructure (DECISION G v1.5): all plugin files moved into headless-sync/; CLAUDE.md, STATUS.md, docs/ remain at root; composer.json PSR-4 fixed to explicit per-prefix maps (FLAG-P0S1-2 resolved); vendor autoload stubs regenerated; workspace-root .gitignore added; git repo initialized, remote wired, committed. Push blocked: SSH key not on this machine (FLAG-MONOREPO-SSH).
2026-06-22 | P0-S2 | Shipped: core/Migrations/ engine (MigrationRunner with UUIDv7 per ADR-015, AbstractSqlMigration, MigrationRecord, ConnectionInterface + WpdbMysqlConnection + PgsqlConnection + ConnectionFactory, MigrationException); 12 concrete migration classes (2 MySQL, 10 PgSQL) in database/Core/; MigrationServiceProvider wired into ContainerBuilder; phpunit.xml; tests/Unit/Migrations/ (MigrationRunnerTest, AbstractSqlMigrationTest, FakeConnection, FakeMigration); composer.json + vendor stubs updated (HSP\Database\, HSP\Tests\ namespaces). Review corrections applied: UUIDv7 replaces UUIDv4, bootstrap() single-sourced to 0008 SQL file (no inline DDL copy), CHAR(64) confirmed correct per OPEN-6 v1.3 for MySQL only, numeric-prefix ordering guard test added, checksum prefix-stability tests added, idempotency tests added. All DoD Gates 1–6 verified and approved. | No new flags.
2026-06-22 | P0-S3 | Shipped: core/Module/ (ModuleManifest, ModuleDiscovery, ModuleLoader, ModuleRegistry, ModuleRegistrar, Exception/InvalidManifestException), core/Contracts/ModuleInterface.php (OPEN-9 union shape), core/Container/Definitions/ModuleServiceProvider.php, modules/Content/module.json fixture, tests/Unit/Module/ (35 tests). 57/57 unit tests pass. Two-phase register-then-boot ordering verified across modules. | Flags: FLAG-P0S3-1 (core/Module singular, session map wins — no action); FLAG-P0S3-2 (phpunit ^11.5 require-dev, Accepted); BOM fix in MigrationRunner.php (P0-S2 file, benign).
2026-06-22 | housekeeping | Committed P0-S2+P0-S3 (close ritual had been skipped; tree was dirty). SSH verified; pushed to origin/main (608fb27). FLAG-MONOREPO-SSH resolved.
2026-06-22 | P0-S4 | Shipped: core/Contracts/ (OutboxWriterInterface, AggregateVersionCounterInterface), core/Events/Outbox/ (OutboxEvent, OutboxWriter, AggregateVersionCounter, Exception/OutboxWriteException, Connection/OutboxConnectionInterface + MysqliOutboxConnection + PgsqlOutboxConnection), core/Workers/Strategies/RelayWorkerStrategy, core/Container/Definitions/OutboxServiceProvider (wired into ContainerBuilder), tests/bootstrap.php (wpdb stub), tests/Unit/Events/Outbox/ (FakeWpdb, FakeOutboxConnection, AggregateVersionCounterTest ×5, OutboxWriterTest ×8, RelayWorkerStrategyTest ×21), tests/Integration/Events/Outbox/ (ConcurrentAggregateVersionTest ×3 live MySQL, RelayEndToEndTest ×5 live MySQL + live PG). Bugs fixed: bare VALUES(1) → LAST_INSERT_ID(1) in AggregateVersionCounter; \wpdb type hint → object for structural test compatibility; bind_param type-string mismatch in test setup. All four P0-S4 DoD items proved against live DBs: happy-path relay, idempotent re-relay (ON CONFLICT DO NOTHING), crash-safety (CommitSaboteurMysqlConnection — PG row survives MySQL rollback, recovery tick produces no duplicate), SKIP LOCKED concurrency. RelayWorkerStrategy redesigned mid-session: removed 'relaying' intermediate status; MySQL FOR UPDATE lock spans entire batch (BEGIN→SELECT SKIP LOCKED→PG insert+mark-relayed→COMMIT). Full suite: 99/99 tests pass (91 unit + 8 integration). Reviewer approved. | FLAG-P0S4-1: resolved by redesign (no DDL change). FLAG-P0S4-2: resolved (live-DB integration tests pass). FLAG-P0S4-3: open — created_at UTC fidelity on relay binding and assertion; resolve by P0-S7 gate.
2026-06-22 | P0-S5 | Shipped: core/Queue/Exception/QueueException, core/Queue/Providers/Database/ (QueueConnectionInterface, DatabaseQueueConnection, DatabaseQueueProvider), core/Container/Definitions/QueueServiceProvider (wired into ContainerBuilder), tests/Unit/Queue/ (FakeQueueConnection, FakeEvent, DatabaseQueueProviderTest ×37), tests/Integration/Queue/DatabaseQueueProviderIntegrationTest ×10 (live PG). Bugs fixed: (1) DatabaseQueueConnection was final — extracted QueueConnectionInterface; (2) pg_connect() pooling on SKIP LOCKED test — fixed with PGSQL_CONNECT_FORCE_NEW; (3) ownership-fencing bug — complete(), release(), deadLetter() were fenced only on status='claimed', not worker_id — fixed by adding AND worker_id=$workerId fence, returning bool (false=lease lost, abandon), moving deadLetter() ownership UPDATE first inside the transaction. New integration test proves fencing: A claims, lease expires, requeueTimedOut revives, B claims, A's complete() and release() both return false, J remains owned by B with attempts=2. Full suite: 145/145 tests pass (127 unit + 10 PG integration + 8 pre-existing relay integration). | FLAG-P0S5-1 raised and resolved same session via DECISION E (v1.5): three pg_* wrappers are accepted temporary duplication; consolidate to shared DatabaseConnectionInterface in P0-S7; P0-S6 must introduce no new raw pg_* wrapper.
2026-06-22 | P0-S6 | Shipped: core/Workers/WorkerStrategyInterface, WorkerExecutionContext, HeartbeatRecord, HeartbeatPublisherInterface, NullHeartbeatPublisher, WorkerEngine; core/Workers/Strategies/ EventWorkerStrategy (full Doc 8 §7 pipeline: Claim→Load→Validate→Resolve→Execute→Commit→Ack, retry/backoff/deadLetter), ReplayWorkerStrategy stub, ReconciliationWorkerStrategy stub, MaintenanceWorkerStrategy stub; core/Events/EventRegistry (explicit registration, OPEN-1 naming validation); core/Delivery/AdapterRegistry (explicit registration, last-wins on duplicate); core/Container/Definitions/WorkerServiceProvider (wired into ContainerBuilder). Tests: tests/Unit/Workers/ (WorkerEngineTest ×14, EventWorkerStrategyTest ×14, FakeHeartbeatPublisher, FakeWorkerStrategy, FakeQueueProvider), tests/Unit/Events/EventRegistryTest ×15, tests/Unit/Delivery/ (AdapterRegistryTest ×12, FakeAdapter). Full suite: 179/179 unit tests pass. No new pg_* wrapper introduced (DECISION E enforced). Pre-existing PHPUnit 12 deprecation in DatabaseQueueProviderTest (@dataProvider doc-comment) carried over unchanged. | No new flags.
2026-06-22 | P0-S7 | Gate verification: all 6 DoD Gate items confirmed pass (type canon, LAST_INSERT_ID counter, no postmeta refs, TIMESTAMPTZ/DATETIME split, VARCHAR(64)/CHAR(64) checksums, UUID worker-identity). DECISION E consolidation: core/Database/ introduced (DatabaseConnectionInterface, PostgresDatabaseConnection, DatabaseException); OutboxConnectionInterface and QueueConnectionInterface collapsed to extend DatabaseConnectionInterface; PgsqlOutboxConnection and DatabaseQueueConnection now delegate to shared PostgresDatabaseConnection (no duplicate pg_* logic); migration engine untouched. FLAG-P0S4-3 resolved: RelayWorkerStrategy '+00:00' binding confirmed; RelayEndToEndTest assertion strengthened to full UTC datetime + explicit offset regex. Full suite: 198 tests, 180 pass, 18 skipped (integration, live DB not in CI), 0 failures. | FLAG-P0S7-1 raised: DECISION E collapse interpretation ambiguity.
2026-06-23 | P0-S7 (continued) | DECISION E v1.6 — Split ruling applied: OutboxConnectionInterface and QueueConnectionInterface deleted; QueueConnectionInterface collapsed fully into DatabaseConnectionInterface; MysqlOutboxConnectionInterface introduced (MySQL capture path, no PG dependency); PgsqlOutboxConnection now implements DatabaseConnectionInterface via composition; DatabaseQueueConnection implements DatabaseConnectionInterface via composition; RelayWorkerStrategy holds explicit MysqlOutboxConnectionInterface + DatabaseConnectionInterface; PostgresDatabaseConnection::rollback() swallow semantics verified and unit-tested; all fakes split (FakeMysqlOutboxConnection, FakePgsqlOutboxConnection, FakeQueueConnection updated); CommitSaboteurMysqlConnection + integration test QueueConnectionInterface references updated; ARCHITECTURE_DECISIONS.md DECISION E bumped to v1.6 with full ruling. PostgresDatabaseConnectionTest added (8 tests including rollback swallow invariant). Full suite: 204 unit / 18 integration — 222 total, 0 failed, 0 skipped. | FLAG-P0S7-1 closed — DECISION E v1.6.
2026-06-23 | P1A-S1 | Shipped: modules/Content/Events/ContentEventTypes.php (9 OPEN-1 constants + ALL list), modules/Content/EventProvider.php (implements EventProviderInterface, delegates to OutboxWriterInterface), modules/Content/HookWiring.php (7 WP hooks, membership-based public-set capture per OPEN-10), modules/Content/ContentModule.php (implements ModuleInterface). Tests: tests/Unit/Content/ (ContentEventTypesTest ×57, ContentEventProviderTest ×36, HookWiringTest ×48, FakeOutboxWriter). OPEN-10 ruling applied: transition matrix uses $wasPublic/$isPublic booleans; all exit transitions emit .deleted; wp_trash_post suppressed by $handledByTransition guard when transition already fired. Full suite: 363 unit, 0 failed. | FLAG-P1AS1-1 resolved (OPEN-10 Resolved).
2026-06-23 | P1A-S2 | Shipped: modules/Content/SourceModels/ (PageSourceModel, PostSourceModel, CategorySourceModel — all readonly/immutable, strongly typed, no canonical model shape); modules/Content/Extractors/ (PageExtractor, PostExtractor, CategoryExtractor — accept already-loaded raw data arrays, no global WP calls, no DB, delegate to validators); modules/Content/Validation/ (PageValidator, PostValidator, CategoryValidator — fail-fast on missing ID/slug/status/type, collect multiple violations into ValidationException.getViolations(); ValidationException typed exception). Tests: tests/Unit/Content/SourceModels/ (PageSourceModelTest ×4, PostSourceModelTest ×4, CategorySourceModelTest ×4); tests/Unit/Content/Extractors/ (PageExtractorTest ×20, PostExtractorTest ×22, CategoryExtractorTest ×17); tests/Unit/Content/Validation/ (PageValidatorTest ×10, PostValidatorTest ×13, CategoryValidatorTest ×11). P1A-S2 tests: 247 clean, 0 deprecations. Full unit suite: 451 tests, 0 failed, 1 pre-existing deprecation (@dataProvider doc-comment in DatabaseQueueProviderTest, carried from P0-S5). No DB dependency; no WordPress function calls in any unit path. | No new flags.
2026-06-23 | P1A-S3 | Shipped: CanonicalPost/Page/Category (implement CanonicalModelInterface; order-insensitive sha256 getChecksum — sort categoryIds, ksort meta, ATOM timestamps, \0 separator, pinned digests); PostTransformer/PageTransformer/CategoryTransformer (pure SourceModel→CanonicalModel, title trimmed, other strings verbatim); tests/Unit/Content/Transformers/ (PostTransformerTest ×13, PageTransformerTest ×14, CategoryTransformerTest ×14) + tests/Unit/Content/CanonicalModels/ (CanonicalPostTest ×11, CanonicalPageTest ×11, CanonicalCategoryTest ×10, incl. order-independence + pinned-digest tests). meta flat-scalar invariant confirmed enforced at extraction boundary (P1A-S2 extractors cast all values to string; ksort sufficient, no recursive normalisation needed). 528 tests, 0 failed, 0 skipped, 1 pre-existing deprecation. | FLAG-P1AS3-1 raised: open, deferred to P1A-S4 kickoff — DECISION 3 write-suppress compatibility: architect must rule Option A (adapter uses canonical.getChecksum() directly) or Option B (adapter computes separate projection-shaped checksum) before wiring write-suppress.
2026-06-23 | housekeeping | OPEN-11 recorded in ADR (Option A, lossless Phase 1A projection, canonical checksum authoritative); FLAG-P1AS3-1 resolved. No code change.
2026-06-24 | P1A-S6c | Delivery connection isolation (DECISION K). Shipped: DeliveryServiceProvider (core/Container/Definitions/DeliveryServiceProvider.php — PGSQL_CONNECT_FORCE_NEW, wraps PostgresDatabaseConnection, no new pg_* wrapper); QueueServiceProvider DatabaseConnectionInterface singleton binding removed; ContainerBuilder registers DeliveryServiceProvider before WorkerServiceProvider and ContentServiceProvider; DeliveryConnectionIsolationTest (5 tests, 23 assertions — DoD-4 items i–v: PID-verified physical handle distinctness via SELECT pg_backend_pid() on each handle (relay PID ≠ delivery PID, delivery PID stable across sequential reads); Resolve-stage reads via delivery connection; relay uncommitted INSERT cannot expose state or block a Resolve-stage read (non-blocking wall-clock bound < 1 s confirmed; B(3) synthetic lock-contention negative control — throwaway row INSERT+COMMIT, relay UPDATE holds row lock, delivery SELECT FOR UPDATE blocked and cancelled by statement_timeout=300ms, SQLSTATE 57014 "statement timeout" confirmed, elapsed >= 300 ms); query provider executes via delivery connection; adapter persist under DECISION 3 txn boundary). DECISION K recorded in ARCHITECTURE_DECISIONS.md v1.11 (constrains DECISION E, satisfies DECISION J). IMPLEMENTATION_PLAN.md §5b: P1A-S6c inserted, P1A-S6 Depends-on = P1A-S6c. Suite: 789/789, 0 failures, 0 skipped (live DB). FLAG-P1AS6A-1 resolved. | no new flags.
2026-06-25 | P1A-S6d | Dispatcher stage (system.events → system.queue_jobs, DECISION L v1.12→v1.13). Shipped: DECISION L recorded (v1.12, 2026-06-25) + P1A-S6d inserted in IMPLEMENTATION_PLAN.md §5b before P1A-S6; migration 0011_add_unique_event_id_to_queue_jobs.sql (UNIQUE(event_id) on system.queue_jobs — frozen 0003 untouched); DatabaseQueueProvider::enqueueIdempotent() (ON CONFLICT(event_id) DO NOTHING); QueueServiceProvider DatabaseQueueProvider concrete alias; DispatchBatch, EventDispatcher (anti-join claim: NOT EXISTS + FOR UPDATE SKIP LOCKED + LIMIT N; queue name hardcoded 'content'; no event_type-prefix routing); DispatcherWorkerStrategy (WorkerStrategyInterface); DispatcherServiceProvider (own FORCE_NEW 'dispatcher.connection.pgsql' — NOT DatabaseConnectionInterface/DECISION K delivery handle, no new pg_* wrapper class); ContainerBuilder registers DispatcherServiceProvider after QueueServiceProvider. Integration tests: DispatcherIntegrationTest (8 tests — happy path, empty-queue false-return, NOT EXISTS idempotency, completed-event-not-redispatched, SKIP LOCKED concurrency, relay→dispatcher→queue link (MySQL+PG), worker-claim routing proof, FORCE_NEW PID-distinctness assertion). Blockers resolved: (1) CONNECTION — dispatcher opens own FORCE_NEW handle, does not reuse delivery singleton; (2) QUEUE ROUTING — prefix routing removed, hardcoded 'content'; (3) TEST ISOLATION — all integration test tearDown()s fixed: $pgConn = null after pg_close(); AdapterAtomicityIntegrationTest::createSchema() adds defensive DROP SCHEMA at start to eliminate cross-run residue. Suite: 797 tests, 1716 assertions, 0 failures, 0 skipped, 1 pre-existing deprecation — confirmed deterministically green on two consecutive cold runs with both MySQL (127.0.0.1:10053) and PG (127.0.0.1:5432) live. ARCHITECTURE_DECISIONS.md bumped to v1.13 (amendment-log v1.12 summary corrected; v1.13 entry records reconciliation). | FLAG-P1AS6D-1 raised: no relay/queue runtime PG handle exposed as container binding post-S6c; dispatcher opens its own fourth FORCE_NEW handle ('dispatcher.connection.pgsql') — connection-topology decision pending architect ratification (DECISION K follow-up).
2026-06-24 | P1A-S6b | Shipped: WpContentLoader contract + WpContentLoaderImpl (get_post/get_post_meta/get_term/wp_get_post_terms); AdapterInterface::tombstone() + PageAdapter/PostAdapter/CategoryAdapter tombstone impls (soft-delete, DECISION I, DECISION 3 atomicity, deleted_at = source_updated_at); WpContentLoaderImpl shape matched by FakeWpContentLoader (all extractor-consumed keys identical); ContentSubscriber (9-type routing, OPEN-1 types, RuntimeException on missing handler); ContentUpsertHandlerInterface; Page/Post/CategoryUpsertHandler (loader→extractor→transformer→adapter pipeline, DECISION H Option B); Page/Post/CategoryTombstoneHandler (event-envelope-only, no WP reload); EventWorkerStrategy::executeHandler() un-stubbed (EventRegistry handler dispatch); EventWorkerStrategy Resolve-stage stale guard added (PRIMARY gate, DECISION J Layer 1 — non-locking SELECT on system.aggregate_versions, <=stored → ack + zero writes); WorkerServiceProvider wired DatabaseConnectionInterface to EventWorkerStrategy (DECISION E, no new pg_connect, queue FORCE_NEW handle not entangled); ContentModule + ContentServiceProvider wired all 9 handlers + ContentSubscriber into EventRegistry. Integration tests: HandlerSpineIntegrationTest (12 tests — persist×3, tombstone×4, idempotent re-delivery×2, adapter GREATEST guard×1, subscriber routing×2; adapter stale write-set assertions strengthened: stale event's own processed_events row proven by ID, aggregate_versions row count asserted); ResolveStageGuardIntegrationTest (4 tests — zero-writes+no-handler+job-acked on stale event proven on live PG, non-stale does not fire, equal-version treated as stale, missing aggregate_versions row not stale). Unit tests: ContentHandlerTest (9), ContentSubscriberTest (3), adapter tombstone unit tests added to Page/Post/CategoryAdapterTest (6 each = 18 total). Suite: 784 tests, 1664 assertions, 0 failures, 0 errors, 0 skipped, 1 pre-existing deprecation. DECISIONS H/I/J recorded in ARCHITECTURE_DECISIONS.md (step 0 of session). FLAG-P1AS6A-1 blast radius extended: now covers worker Resolve-stage PG read path in addition to REST delivery; still open, still E2E-blocking. FLAG-P1AS6-1 fully resolved. | no new flags.
2026-06-23 | P1A-S4 | Shipped: modules/Content/Migrations/ (content schema; pages/posts/taxonomies/entity_taxonomies; TIMESTAMPTZ + VARCHAR(64) canon; entity_taxonomies pure join table) and modules/Content/Adapters/ (Page/Post/Category persist() — DECISION 3 three-op atomic txn; OPEN-11 Option A canonical-checksum write-suppress; in-txn lockAggregateVersion() FOR UPDATE guard; monotonic GREATEST() aggregate_versions; full-replace entity_taxonomies rewrite). bulkPersist() fail-fast LogicException stub (Phase 1A). Tests: unit adapter suites + live-PG atomicity/idempotency/join-rewrite/interleave integration. Item-6 TOCTOU race fixed (version guard moved inside txn behind FOR UPDATE). Suite 598/598. | Flags resolved: FLAG-P1AS4-1 (pure join table), FLAG-P1AS4-2 (monotonic guard), FLAG-P1AS4-3 (bulkPersist Phase 1A throwing stub) — all architect-ruled 2026-06-23.
2026-07-15 | OPSC-S1 | Operations Console core scaffolding SHIPPED — read-only, no UI, no actions (DECISION V (a)/(h)/(i); ADR-047/048/052/053; ADR-051 HELD, not cited). Pre-flight PASS: confirmed the six ratified ADR bodies (047/048/049/050/052/053) exist in ARCHITECTURE_DECISIONS.md and are consistent with DECISION V clauses (a)-(j) before scaffolding. Contracts (`core/Contracts/Operations/` — import nothing outside `core/Contracts/`, Rule 5 verbatim, grep-verified): registry contracts PageRegistryInterface/NavigationRegistryInterface/WidgetRegistryInterface/ActionRegistryInterface/AssetRegistryInterface + descriptors ConsolePage/NavigationItem/ConsoleWidget/ConsoleAction/ConsoleAsset; provider contracts HealthProviderInterface/MetricsProviderInterface/WorkerStatusProviderInterface/QueueStatusProviderInterface/EndpointProviderInterface over OperationsProviderInterface (key()) + DTOs HealthReport/MetricSample/WorkerStatus/QueueStatus/EndpointDescriptor + Severity scale (ADR-049). Infra (`core/Operations/`, lowercase — DECISION V (i)): Registries/{Page,Navigation,Widget,Action,Asset}Registry (explicit registration only — no reflection/scanning; duplicate → LogicException, unknown-key → RuntimeException; position-then-registration ordering; mirrors EventRegistry/AdapterRegistry idiom); Services/ConsoleStateStore (request-scope snapshot cache — zero persistence, zero PG; not a JS state store — DECISION V (a)), Services/RefreshCoordinator (centralizes provider refresh into the store → widgets never poll independently, one provider call per refresh; explicit non-reflective snapshot extraction via match over the five known provider interfaces; rejects a provider that implements only the base interface), Services/OperationsService (the single UI-facing seam — ADR-053; ctor takes only core contracts + core services, so no DatabaseConnectionInterface/concrete provider/queue provider is reachable from a would-be UI caller). Wiring: new core/Container/Definitions/OperationsServiceProvider (all singletons, constructor injection — ADR-012/Rule 7) registered in ContainerBuilder before ContentServiceProvider; opens NO PG handle, NO pg_* wrapper, NO fifth handle (DECISION L Ruling 0 topology unchanged — grep-verified, only doc-comment mentions). Tests: 55 new Unit under tests/Unit/Operations/ — PageRegistryTest/NavigationRegistryTest/WidgetRegistryTest/ActionRegistryTest/AssetRegistryTest (register/has/get, duplicate-throws, unknown-key-throws, empty-field validation, position ordering, forPage filtering), SeverityTest, ConsoleStateStoreTest, RefreshCoordinatorTest (one-call-per-refresh, snapshot routing for all five provider kinds, refreshOne, duplicate-key + bare-provider rejection), OperationsServiceTest (discovery passthrough, refreshAll, lazy single-provider refresh cached, widget→providerKey resolution, TWO widgets over one provider = exactly one provider call, unknown-widget throws), OperationsServiceProviderTest (every binding resolves via constructor injection as singletons; store shared as singleton). Full Unit suite 862/862 green, 1780 assertions, 0 failures, 0 deprecations, deterministic on two consecutive runs (was 807 + 55). Verified out-of-scope untouched: no concrete providers (OPSC-S2), no UI/wp-admin (OPSC-S3), no action behaviour (OPSC-S4), no migrations, no module code, no PG reads; `git status --porcelain` shows only core/Contracts/Operations/, core/Operations/, OperationsServiceProvider, the one-line ContainerBuilder edit, and tests/Unit/Operations/. | no new flags.
2026-07-15 | OPSC-S2 | Diagnostics + Metrics providers SHIPPED — current-state, derived-only (DECISION V (c)/(g)/(h); DECISION P/Q; ADR-049; OPEN-8). Core reader core/Operations/Diagnostics/OperationsQueryReader (single delivery-handle read seam; every method a SELECT over existing frozen tables; zero DML; reuses delivery DatabaseConnectionInterface — DECISION K/V (g), no fifth handle DECISION L Ruling 0, no new pg_* wrapper DECISION E). Four core providers core/Operations/Providers/: QueueStatusProvider (queue depth + DLQ depth + oldest-pending age), WorkerStatusProvider (DECISION P — offline = last_heartbeat_at-age query vs config threshold; read-only, no lifecycle DECISION V (f)), MetricsProvider (derived samples: queue/DLQ/worker counts, processing-rate over config window, replay pending/completed, reconciliation backlog — zero persistence DECISION V (c)), HealthProvider (ADR-049 severity rollup + DB-unreachable → CRITICAL). System Information (SystemInformation + SystemInformationProvider, Doc 12 §13): platform/PHP/WP/PG versions + queue provider + migration state + module versions (OPEN-8). Module Inspector (Doc 12 §14): NEW additive core contract ModuleInspectionProviderInterface + immutable ModuleInspection DTO under core/Contracts/Operations/ (architect ruling 2026-07-15 Option A) — descriptive metadata only, no query on inspection path; deliberately NOT an OperationsProviderInterface → stays OFF the RefreshCoordinator snapshot path so the five-kind match is untouched (chose "directly-queried diagnostics service", constraint 4); core ModuleInspector aggregator (explicit injection). Module ops modules/Content/Operations/ (Rule 5 — depend on core/Contracts/Operations/ only): ContentModuleInspection (version from module.json), ContentEndpointProvider (six hsp/v1 routes behind EndpointProviderInterface), ContentMetricsProvider (live content.* projection counts on delivery handle). Wiring: OperationsServiceProvider gains reader + 4 core providers + System Info + ModuleInspector and registers the 4 core providers with the RefreshCoordinator in boot(); ContentServiceProvider registers its metrics/endpoint providers + module descriptor in boot(). Config worker.php: heartbeat.offline_after_seconds=60, console.processing_rate_window_seconds=300 (config-driven, no schema, no persistence). Tests: 41 new Unit (readers/providers over a scripted read-only fake that throws on any write; wiring smoke — 4 core keys register with coordinator) + 9 live-PG Integration (live depths/DLQ/oldest-pending, heartbeat-age online/offline, derived point-in-time metrics, OPEN-8 version reads, content projection counts, four-handle topology = 4 DISTINCT backend PIDs with provider reads on the delivery PID and no fifth, and zero DML via write-spy over every provider). Full Unit 903/903 green, 0 failures/warnings/deprecations (was 862 + 41), deterministic ×2; OPSC-S2 Integration 9/9 ×2; full Integration 131 pass / 37 env-skipped / 0 failures except GATE-S4 crit-1's `git status --porcelain -- core/` guard, which is red only because this session's in-scope core/ files are uncommitted (clears on commit — FLAG-OPSCS2-1). No OPSC-S1 contract/registry/service file modified (two additive contract files only); no UI (OPSC-S3), no actions (OPSC-S4), no migration, no new table. | FLAG-OPSCS2-1 raised (informational — GATE-S4 dirty-tree snapshot; clears on commit).



2026-07-16 | ONB-S0 | DECISION W Ratification (docs-only; OPSC-S0 pattern). Adopted the Onboarding / First-Run feature into the frozen record via **DECISION W**. ARCHITECTURE_DECISIONS.md v1.20->v1.21: amendment-log row + DECISION W recording six architect rulings (2026-07-16) — (a) UI stack: **DECISION V (a) AMENDED globally — React+shadcn adopted as THE admin UI stack**; build-artifact policy = commit dist/ to repo (npm build in dev/CI only, production deploy = file copy, no host build step); WPCS security extended to the REST/ajax endpoints the React app calls; shipped OPSC-S1..S4 server-rendered PHP console NOT rewritten, only new admin UI (incl. onboarding) is React; DECISION V provider/registry architecture unchanged. (b) backfill = full-reconciliation re-emission via ReconciliationService::reconcileFull() (DECISION U) through the normal outbox->relay->dispatch->worker pipeline — NO direct WP->PG copy, no second repair path, write-spy proof in ONB-S2 DoD. (c) queue drain = live worker heartbeat is a HARD PREREQUISITE (DECISION P age check), workers drain as normal, no in-request tick drain. (d) progress derived on-demand (DECISION Q), completion = single WP option hsp_onboarding_state in MySQL, zero new persistence / no schema change. (e) placement = core/Onboarding/ (NOT core/Operations/ — keeps V (j) console-is-observability-only), delegate-only, no PG handle of its own (K reuse; L Ruling 0; E). (f) nav gating — Operations + API Playground hidden until hsp_onboarding_state=complete; five preflight checks (pgsql ext, PG constants, PG reachable, migration-engine state, PHP version) hard-block progression. DECISION V (a) annotated with an amended-by-W pointer (the two do not silently contradict). Implications table +7 DECISION W rows. IMPLEMENTATION_PLAN.md §5b: inserted ONB-S1 (preflight + onboarding page + nav gating + completion flag) and ONB-S2 (backfill trigger + derived live progress + redirect to Operations) rows (scope/authority/DoD/depends-on), sequenced after OPSC-S4 before Phase 1B; §5b closing note updated to record the V (a) amendment as explicit/ratified. CLAUDE.md: folder structure +core/Onboarding/ and reconciled core/Operations/ + resources/ notes to the React amendment; Coding Standard +DECISION W (a) admin-UI-stack + WPCS-at-ajax-boundary note; SETTLED +DECISION W line and annotated the V UI clause as superseded. STATUS pointer -> ONB-S1. Docs-only — no production code, migrations, or tests. | ACKNOWLEDGED AMENDMENT (not a silent conflict): DECISION W (a) amends frozen DECISION V (a) — architect explicitly chose the amendment path (React+shadcn, commit dist/, whole admin UI). No other new flags.
2026-07-16 | ONB-S1a | Frontend toolchain bootstrap + onboarding page skeleton (DECISION W (a)/(e); DECISION V (b)/(j); DECISION K/L Ruling 0/E; ADR-012; Rule 5). STEP 0 (docs): split §5b ONB-S1 into ONB-S1a (this session) + ONB-S1b (next; remaining DoD verbatim, depends-on S1a); ONB-S2 depends-on -> ONB-S1b; appended a styling addendum under DECISION W (a) (dark theme is the DEFAULT via shadcn `.dark`, on the plugin mount root only — never wp-admin `<body>`; OPSC console styling unchanged until a future migration). Frontend (`resources/admin-ui/`, npm): package.json + package-lock.json; Vite 5 IIFE/library build -> committed `dist/hsp-onboarding.{js,css}` (stable non-hashed filenames, deterministic PHP enqueue); React 18 bundled (self-contained, no CDN, no host build step); Tailwind 3 + shadcn tokens with preflight DISABLED and utilities scoped via `important: '.hsp-admin-ui'` + a mount-scoped base layer (wp-admin untouched — only global rule sets `--tw-*` vars, zero visual resets); src/ (main.tsx mount seam applies `.hsp-admin-ui dark` on the mount root only; App skeleton; Card/Button; bootstrap.ts reads localized nonce/REST base). PHP registrar (`core/Onboarding/`): OnboardingPageController registers ONE capability-gated wp-admin page, renders the `#hsp-admin-ui-root` mount shell, enqueues committed dist/ + localizes `HSP_ONBOARDING` (nonce/REST/version), output-escaped + capability-guarded (V (b)); OnboardingAdminRegistrar = thin hook registrar. Placed in core/Onboarding/, not core/Operations/ (W (e); V (j) console unaffected). Wiring: new OnboardingServiceProvider (singletons, constructor injection) in ContainerBuilder; headless-sync.php binds the registrar on plugins_loaded(20). .gitignore: node_modules/ ignored, `!.../admin-ui/dist/` guard so the committed bundle is tracked. CLAUDE.md: real npm frontend build-command rows (cited to W (a)). Tests: 8 new Unit (page registration, custom-cap, mount-shell render, capability-deny, committed-dist enqueue + bootstrap localization, no-enqueue on unrelated pages; wiring singletons + registrar no-op) + `rest_url` test stub. Full Unit 973/973 green (was 965 + 8), 0 deprecations; full suite 1110 tests, 0 failures, 121 DB-env-skipped (GATE-S4 git guard clears on commit — FLAG-OPSCS2-1). Verified: NO PG handle, NO new pg_* wrapper, NO schema change, NO OPSC file modified; git status shows only in-scope paths. | No new flags.